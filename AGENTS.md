# AGENTS.md — Mousefall

> **Вся документация в этом проекте ведётся на русском языке.**
> Этот файл — точка входа для AI-агентов.

---

## Навигация

**Полная документация для агентов:** [`docs/agents/INDEX.md`](./docs/agents/INDEX.md)

Если ты агент, начни с `docs/agents/INDEX.md` — там карта всех файлов и указания, куда идти за чем.

**Работаешь в конкретной папке `src/X/`?** → сначала прочитай `src/X/AGENTS.md` (локальные правила слоя).

---

## Локальные правила по папкам

| Папка | Локальный AGENTS.md |
|-------|---------------------|
| `src/simulation/` | [`src/simulation/AGENTS.md`](./src/simulation/AGENTS.md) |
| `src/presentation/` | [`src/presentation/AGENTS.md`](./src/presentation/AGENTS.md) |
| `src/ui/` | [`src/ui/AGENTS.md`](./src/ui/AGENTS.md) |
| `src/content/` | [`src/content/AGENTS.md`](./src/content/AGENTS.md) |
| `src/i18n/` | [`src/i18n/AGENTS.md`](./src/i18n/AGENTS.md) |

---

## Язык документации

**Вся документация, комментарии к кода и текстовые сопровождения в проекте должны писаться на русском языке.**
Исключение: имена переменных, типов, функций и ключей в коде остаются на английском.

### Комментарии в коде

- Все JSDoc-, однострочные (`//`) и многострочные (`/* */`) комментарии в исходном коде (`*.ts`, `*.tsx`, `*.js`, `*.jsx`) пишутся на русском языке.
- Закомментированный код (временно отключённые фрагменты) переводить не требуется, но если внутри него есть текстовые пояснения — они тоже должны быть на русском.
- `eslint-disable` и прочие директивы линтера остаются на английском (это технические метки).
- Содержимое `.describe()` в Zod-схемах считается комментарием к коду и также переводится на русский.

# Правила работы агента

## Уточняющие вопросы

Используй `AskUserQuestion` только для одного простого уточняющего вопроса, без которого невозможно продолжить работу.

Если требуется задать несколько вопросов, **не используй `AskUserQuestion`**. Вместо этого выведи их обычным сообщением в виде нумерованного списка и дождись ответа пользователя.

Запрещается вызывать `AskUserQuestion` несколько раз в одном ответе.

После вызова `AskUserQuestion` или после вывода списка вопросов остановись и дождись ответа пользователя. Не делай предположений, не продолжай выполнение задачи и не вызывай другие инструменты до получения ответа.

## Работа с неизвестной информацией

Если информации недостаточно, сначала запроси необходимые сведения у пользователя. Не выдумывай отсутствующие данные и не принимай решения на основе предположений.