# Диаграмма зависимостей боевой системы

> Карта связей между типами урона, действиями, правилами экипировки, статусами и тайловыми эффектами.
> Диаграмма в формате Mermaid — её можно открыть в любом редакторе с поддержкой Mermaid или на [mermaid.live](https://mermaid.live).

---

## Общая схема зависимостей

```mermaid
flowchart LR
    subgraph DamageTypes["Типы урона"]
        SLASH["damage.physical.slashing"]
        BLUNT["damage.physical.blunt"]
        PIERCE["damage.physical.piercing"]
        FIRE["damage.magical.fire"]
        ELECTRIC["damage.magical.electric"]
    end

    subgraph CombatTags["Боевые теги"]
        MELEE["attack.melee"]
        RANGED["attack.ranged"]
        CRIT["attack.crit"]
    end

    subgraph EquipRules["Правила экипировки"]
        RULE_PIERCE["piercing_crit<br/>MODIFY_DAMAGE, addTags"]
        RULE_BLUNT["blunt_melee_stun<br/>APPLY_STATUS"]
        RULE_BLUNT_PUSH["blunt_push_wall_stun<br/>APPLY_STATUS"]
        RULE_SLASH["slashing_causes_bleeding<br/>APPLY_STATUS"]
    end

    subgraph WorldRules["Мировые правила"]
        RULE_COLLISION_DAZE["collision_daze<br/>APPLY_STATUS"]
        RULE_COLLISION_DAMAGE["collision_damage<br/>DEAL_DAMAGE"]
    end

    subgraph Statuses["Статусы на акторе"]
        BLEEDING["bleeding"]
        STUNNED["stunned"]
        DAZED["dazed"]
        BURNING["burning"]
        WET["wet"]
        OILED["oiled"]
        FROZEN["frozen"]
        POISONED["poisoned"]
    end

    subgraph TileEffects["Тайловые эффекты"]
        TILE_WATER["water"]
        TILE_OIL["oil (может быть ignited)"]
        TILE_FOG["fog"]
    end

    SLASH --> RULE_SLASH
    RULE_SLASH -->|30% шанс| BLEEDING

    BLUNT --> RULE_BLUNT
    MELEE --> RULE_BLUNT
    RULE_BLUNT -->|25% шанс| STUNNED

    PUSH["displacement.push"] --> RULE_BLUNT_PUSH
    DAZED --> RULE_BLUNT_PUSH
    RULE_BLUNT_PUSH -->|при dazed| STUNNED

    PUSH --> RULE_COLLISION_DAZE
    RULE_COLLISION_DAZE -->|всегда| DAZED

    PUSH --> RULE_COLLISION_DAMAGE
    RULE_COLLISION_DAMAGE -->|всегда| DAMAGE["урон"]

    PIERCE --> RULE_PIERCE
    RULE_PIERCE -->|20% шанс| CRIT

    FIRE -->|может наложить| BURNING
    ELECTRIC -.->|усиливается на| WET

    TILE_OIL -->|ignited + вход / тик| BURNING
    TILE_WATER -->|вход / тик| WET
    TILE_OIL -->|вход / тик| OILED

    WET ==>|тушит| BURNING
    BURNING ==>|испаряет| WET
    OILED ==>|блокирует| WET
    STUNNED ==>|заменяет| DAZED

    FIRE ==>|поджигает| TILE_OIL
    TILE_OIL ==>|загорается от| FIRE
    TILE_WATER ==>|тушит горящее масло| TILE_FOG
    TILE_WATER -.->|поддерживает| TILE_FOG
```

---

## Легенда

| Стрелка | Значение |
|---|---|
| `--- >` | Прямая причинно-следственная связь: одно порождает другое. |
| `=== >` | Системное взаимоисключение или блокировка: один элемент снимает/блокирует другой. |
| `-.- >` | Косвенное влияние или усиление: элемент модифицирует эффект другого. |

---

## Что показывает диаграмма

### 1. Физические комбо от оружия

- **Колющий урон** → правило `piercing_crit` → шанс 20% крита с тегом `attack.crit`. Множитель крита: `1.5 + dex / 10` (min 1.5, max 3.0).
- **Дробящий урон + ближний бой** → правило `blunt_melee_stun` → шанс `stunned`.
- **Дробящее оружие + толчок в стену** → правило `blunt_push_wall_stun` → `stunned`, если цель уже `dazed`.
- **Рубящий урон** → правило `slashing_causes_bleeding` → шанс `bleeding`.
- **Толчок** → мировое правило `collision_daze` → `dazed` на обоих участниках столкновения.

Все правила экипировки крепятся к шаблонам оружия через `ruleIds` и попадают в `activeRules` актора при экипировке.

### 2. Стихийные комбо

- **Огненный урон** накладывает `burning`.
- **Подожжённое масло** (`oil` с `ignited=true`) накладывает `burning` при входе актора и при тике.
- **Вода** (тайловый эффект) накладывает `wet`.
- **Масло** (тайловый эффект) накладывает `oiled`.
- `wet` тушит `burning`.
- `burning` испаряет `wet`.
- `oiled` блокирует наложение `wet`.
- Электрический урон усиливается, если цель `wet`.

### 3. Взаимодействия тайловых эффектов

- Огненный урон / поджог по клетке с `oil` → `oil` становится `ignited`, происходит взрыв, масло горит и распространяется на соседнее масло.
- `water` + горящее `oil` → масло теряет `ignited`, появляется `fog`.
- `water` + `oil` → сосуществуют.
- `fog` поддерживается водой и рассеивается огненным уроном.

---

## Непоказанные связи

Диаграмма отражает только зафиксированный минимальный набор. Не показаны:

- Сложные физические цепочки: толчок → столкновение → AoE-ударная волна.
- Детали кровотечение + движение → дополнительный урон.
- Все возможные модификаторы от брони, талантов и аур.
- Детали Presentation Layer (как отображаются криты, статусы и тайловые эффекты).

---

## Как отрендерить

- Вставьте код диаграммы на [mermaid.live](https://mermaid.live).
- Или откройте этот файл в редакторе с поддержкой Mermaid (VS Code + плагин Markdown Preview Mermaid Support).
- В GitHub / GitLab Markdown-диаграмма отобразится автоматически.
