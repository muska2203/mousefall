# SYNC_STATUS — Статус рассинхрона документации и кода

> Этот файл помогает агенту понимать, каким системам можно доверять, а где источник правды — код.

---

## Как пользоваться

- Если система `[STABLE]` — код должен соответствовать документации.
- Если система `[DRAFT]`/`[EXPERIMENTAL]` — документация может отставать; источник правды — код + тесты.
- Если нашёл новый рассинхрон — добавь строку в таблицу.
- После устранения рассинхрона — удали строку.

---

## Сводка по системам

| Документ | Статус | Источник правды | Примечание |
|---|---|---|---|
| [`LAYERS.md`](./LAYERS.md) | `[STABLE]` | этот файл | Архитектурные слои не меняются. |
| [`ACTION_SYSTEM.md`](./ACTION_SYSTEM.md) | `[STABLE]` | этот файл для принципов; `src/simulation/core-types.ts` для типов | Архитектура Action → Intent → Event устаканилась. |
| [`TURN_FLOW.md`](./TURN_FLOW.md) | `[STABLE]` | `src/simulation/simulation.ts` | Ход и фракции реализованы. |
| [`CONTENT.md`](./CONTENT.md) | `[STABLE]` | этот файл | Контент-пайплайн TypeScript-шаблоны + Zod устаканился. |
| [`I18N.md`](./I18N.md) | `[STABLE]` | этот файл | Система локализации устаканилась. |
| [`TESTING.md`](./TESTING.md) | `[STABLE]` | этот файл | Правила тестирования устаканились. |
| [`GLOSSARY.md`](./GLOSSARY.md) | `[STABLE]` | этот файл | Термины актуальны. |
| [`PRESENTATION_CONTRACT.md`](./PRESENTATION_CONTRACT.md) | `[STABLE]` | этот файл | Контракт Simulation + Presentation устаканился. |
| [`TILE_EFFECTS.md`](./TILE_EFFECTS.md) | `[STABLE]` | этот файл для процесса; код для реализации | Инструкция по добавлению тайловых эффектов. |
| [`AI_SYSTEM.md`](./AI_SYSTEM.md) | `[DRAFT]` | `src/simulation/ai/` | AI активно дорабатывается; дока может отставать. |
| [`SAVES.md`](./SAVES.md) | `[DRAFT]` | этот файл | Сохранения не реализованы; дока описывает намеренную архитектуру. |
| [`mechanics-overview.md`](../game-design/mechanics-overview.md) | `[STABLE]` | код | Описательный обзор механик и пробелов для геймдизайна; при изменении механик актуализировать по коду. |
| [`progression-concept.md`](../game-design/progression-concept.md) | `[STABLE]` | этот файл | Дизайн прогрессии утверждён (2026-08-03); реализация этапа 0 завершена: все фазы (0.1–0.6) выполнены (2026-08-05), финальное ревью — 2026-08-06. |
| [`equipment-modifiers-concept.md`](../game-design/equipment-modifiers-concept.md) | `[STABLE]` | код + концепт | Подтипы/уровни/аффиксы экипировки и рейнж урона реализованы 2026-08-08; унификация свойств через модификаторы (`fixedModifiers`) — 2026-08-09. |
| [`first-boss-concept.md`](../game-design/first-boss-concept.md) | `[STABLE]` | этот файл | Концепт первого босса утверждён (2026-08-10); реализация **завершена целиком** (2026-08-11): статус `bulwark`, движковый иммунитет, self-buff исполнитель и способность; параметризация swoop + «Налёт» (union `kind` шаблонов способностей); «Удар по земле» (union kind `groundSlam` + правило `ground_slam_daze`); тактика `findCollisionLanding`, стратегия `guardian-boss` + поля `bossStage`/`bossTransitionPending` в AIState, вынос hunter FSM-хелперов в `ai-helpers.ts`, контент `cat_guardian` — 3 способности + HP 90 черновик, документация. Инфраструктура босс-файта реализована (2026-08-14, план `docs/plans/boss-fight-infrastructure.md`): босс спавнится в босс-комнате с запирающимися дверями. |
| [`floor-1-content-concept.md`](../game-design/floor-1-content-concept.md) | `[DRAFT]` | этот файл для дизайна; код для статуса реализации | Концепт контента первого этажа (MVP): две линии сборки (Подвижный боец / Алхимик), тема «подземелье замка», зафиксированные решения по муке/мышеловке/старту/поиску ловушек, список движковых доработок и контента. Реализация: п.1 §3 — генератор переведён на типы комнат (2026-08-14, план `docs/plans/room-types-generation.md`); п.3–4 §3 — статусы `bleeding` и `rooted` (2026-08-18). Числа черновые. Механика пращи детализирована (2026-08-18): дальняя базовая атака с min-range 2, «Бросок камня» с толчком, пассивный стат `throwRange +5` вместо скилла дальнего броска; базовая дальность бросков расходников снижена до 2. Отложенных вопросов не осталось; п.11 §3 (стат `throwRange`) реализован тем же днём; п.9 §3 (дальняя базовая атака: `minRange`/`range` оружия, позиционная форма ATTACK, деградация bump в unarmed, таргетинг в UI) и п.10 §3 (вид способности `throw`) реализованы 2026-08-19 (план `docs/plans/ranged-basic-attack-and-throw.md`); контент пращи реализован тем же днём (шаблон `common_sling`, способность `stone_throw`, модификатор `mod_sling_throw_range`, subtype `sling`). |
---

## Известный рассинхрон

На текущий момент рассинхронов не зафиксировано.

---

## История изменений

| Дата | Что изменилось |
|---|---|
| 2026-07-23 | Завершён аудит фазы 3 (`CONTENT_RULES_EDGE_CASES.md`, `TILE_EFFECTS.md`, `LAYERS.md`, `GLOSSARY.md`). Найденные рассинхроны устранены в документации: добавлен `tileEffectStatus` в порядок мировых слоёв, уточнён список условий/селекторов/эффектов, исправлены пути к текстам и исполнителям интентов. |
| 2026-07-23 | Фаза 4: `CONTENT_RULES_EDGE_CASES.md` укорочен — чеклист добавления правила вынесен в `docs/recipes/add-content-rule.md`. Исправлены устаревшие детали в рецепте и `src/simulation/content-rules/AGENTS.md`. Добавлен рассинхрон по `counterAttack.skillId` в `validation.ts` / тестах. |
| 2026-07-23 | Устранён рассинхрон по `counterAttack.skillId`: удалена мёртвая валидация и тесты на поле, которое не поддерживается `RuleEffect` и runtime. Запись о рассинхроне удалена из `SYNC_STATUS.md`. |
| 2026-07-24 | UI-ревью: устранены зависимости `ui → content` (`TagList`, `settings`), hardcoded боссы вынесены в `GameSession.getDefeatedBosses()`, удалён мёртвый `ConsumablesPanel` и связанные стили, inline-пути рамок заменены на `resolveItemFrame`, поповеры переведены на `usePopoverPosition`, исправлены CSS-переменные и убрано дублирование modal-стилей, `alert()` заменены на неблокирующие toasts, поправлены React-ключи, проведён мелкий рефакторинг (`PhaseButton`, `App.tsx`, `SkillsPanel.tsx`, `PortraitGallery.tsx`), актуализирован `src/ui/README.md`. |
| 2026-07-25 | `fireball` переведён на кастомный шаг `METEOR_FALL` (`src/ui/animation/meteorFallExecutor.ts`); кластерная отрисовка `burning` вынесена в `BurningClusterStrategy` (`src/ui/renderer/tileEffectStatusStrategies/`), `TileEffectStatusRenderer` работает через плагинные стратегии; хардкод цвета горения убран из `src/presentation/animation/builders/tileEffect.ts`. |
| 2026-07-25 | Добавлено поле `affectedEntityId?` в `AnimationStep` (`src/presentation/types.ts`); `getNodeEntityId` в `src/presentation/animation/core/treeBuilder.ts` теперь выводит ID сущности без switch. Добавлено поле `isFieldEvent` в базовый тип `GameEvent` (`src/simulation/core-types.ts`); `FIELD_EVENT_TYPES` удалён из `src/presentation/displayState/planner.ts`, планировщик использует `event.isFieldEvent`. Все создания событий и тесты обновлены. |
| 2026-07-26 | `rain`/`oil_flask` переведены из тестовых скиллов в расходники `water_ball`/`oil_bottle`; `docs/agents/TILE_EFFECTS.md` обновлён — добавлен путь создания источника эффекта через расходник.
| 2026-07-27 | Статусы расширены на объекты окружения (двери и пропы): `DoorTemplate`/`PropTemplate` получили `tags` и `canHaveStatus`, `fire_damage_ignites` теперь поджигает только `flammable`-объекты, актёры от прямого огненного урона не загораются. Обновлены `docs/design/starting-rules-catalog.md` и `docs/agents/CONTENT_RULES_EDGE_CASES.md`.
| 2026-07-29 | Аудит хранения тайлов/эффектов. Исправлены баги жизненного цикла `tileEffects`: сетка теперь пересоздаётся в `SET_MAP` и `generateMap` под фактический размер карты, сохраняется в `FloorSnapshot.tileEffects` и восстанавливается при возврате на этаж (`SetMapIntent` и `FloorTransitionPlan` получили поле `tileEffects`). Введён локальный позиционный индекс сущностей (`buildEntityPositionIndex`, опциональный параметр `index` в read-хелперах `state.ts`) — задействован в FOV, `findPathForPlayer`, AI `moveToward` и автопути в `gameSession`. Обновлены `docs/architecture/TILE_EFFECTS.md` и `src/simulation/AGENTS.md`.
| 2026-07-29 | Слоистая модель клетки, фаза 1 (террейн) реализована: `TileType` → `string` (id террейна), контентная категория `terrains` (схема, загрузчик, реестр, тексты ru/en, манифест, валидация `ruleIds`), шаблоны `floor`/`wall`/`sand` (песок с `moveCost: 2`). Проходимость и `blocksLOS` читаются из шаблона террейна (хелперы `isTerrainWalkable`/`terrainHasTag` в `state.ts`), фильтры «только floor» переведены на тег `ground`; `moveCost` учитывается в `DefaultActionPointCostResolver` (автопуть и AI-pathfinding остаются равностоимостными — зафиксированное ограничение). Рендер террейна — конвенция `/assets/tiles/<id>.png`. Мировой слой `terrain` в `ContentRuleReaction` не входил в фазу и перенесён в фазу 6 плана. Новые доки: `docs/recipes/add-terrain.md`; обновлены `docs/architecture/OVERVIEW.md`, `src/content/AGENTS.md`, `src/simulation/AGENTS.md`, концепт и план миграции. |
| 2026-07-30 | Слоистая модель клетки, фаза 2 (уникальность тайловых эффектов по слою) реализована: `TileEffectLayer` → `'cover' | 'aboveGround'` (foundation отдан террейну), ячейка `TileEffects` хранит эффекты по слою (максимум один эффект каждого слоя), спавн заменяет эффект того же слоя с порядком событий REMOVED → CHANGED. Поля шаблона `blockedByTileEffects`/`mutuallyExclusiveWithTileEffects` удалены (схема и JSON воды/масла). `getTileEffectsAt` возвращает производную запись «ключ — тип эффекта» для читателей (условия правил, реакции). Сортировка оверлеев в presentation — по слою, затем по renderOrder. Обновлены `docs/architecture/TILE_EFFECTS.md`, `docs/agents/TILE_EFFECTS.md`, `docs/recipes/add-tile-effect.md`. |
| 2026-07-30 | Слоистая модель клетки, фаза 3 (air-эффекты) реализована: поле `blocksLOS` в `TileEffectTemplateSchema`; `blocksLOS` в `state.ts` проверяет эффекты клетки после террейна/двери/пропа (слои блокируют обзор независимо); мировая реакция `tileEffectFovReaction` на `TILE_EFFECT_CHANGED` (только `isNew`) и `TILE_EFFECT_REMOVED` порождает `UPDATE_FOG` для эффектов с `blocksLOS` — без неё FOV пересчитывался бы только после хода игрока. Контент: дым (`smoke`, слой `aboveGround`, `blocksLOS: true`) и расходник `smoke_bomb` (решение: полный цикл «предмет → бросок → эффект», не debug-спавн). Обновлены `docs/architecture/TILE_EFFECTS.md`, `docs/agents/TILE_EFFECTS.md`, концепт и план миграции. |
| 2026-07-30 | Концепт слоистой модели клетки пересмотрен: кучи лута исключены (контейнер лута остаётся одиночным предметом); ловушки — одноразовые или постоянные (`oneShot` в шаблоне вместо `charges`), одноразовая уничтожается при срабатывании, постоянная раскрывается и остаётся; обезвреживание и механика поиска ловушек отложены (обнаружение — только срабатыванием и debug-режимом). Обновлены концепт `docs/concepts/cell-layers-model.md` и фазы 4–5 плана `docs/plans/cell-layers-migration.md`. |
| 2026-07-30 | Слоистая модель клетки, фаза 4 (объекты: слоты размещения, точка интереса) реализована. Слоты `solid`/`floorFixture`/`loot` выводятся из типа сущности (`getPlacementSlot` в `src/simulation/state.ts`), единая проверка `canPlaceObjectAt` подключена к генерации этажа, дропу лута, debug-спавну и закрытию двери. Новый вид сущности `poi` (`PointOfInterestEntity`, без `Attackable`): контентная категория `pois` (схема, загрузчик, реестр, тексты ru/en, манифест, валидация `ruleIds`), взаимодействие `use_poi` → интент `ACTIVATE_POI` (процедурный декремент `charges`) → событие `POI_USED`. Мировой слой `object` в `ContentRuleReaction` (порядок слоёв `global → tileEffect → tileEffectStatus → object → tileIntrinsic` зафиксирован в `CONTENT_RULES_EDGE_CASES.md`) собирает `ruleIds` poi на клетке события. Пример — алтарь (`public/content/entities/pois/altar.json`) с разовым лечением через правило `altar_heals_player`. UI: спрайт и рендер poi, поповер, debug-спавн, автопуть до poi, i18n-ключи. Новые доки: `docs/recipes/add-poi.md`; обновлены концепт, план миграции (фаза 4 ✅, ссылки фазы 5), `src/simulation/content-rules/AGENTS.md`, `docs/agents/CONTENT_RULES_EDGE_CASES.md`. |
| 2026-07-30 | Контент-пайплайн мигрирован с JSON на TypeScript: шаблоны перенесены из `public/content/` в `src/content/templates/<категория>/*.ts` (`satisfies XTemplateInput`, Input-типы через `z.input<>` в `schemas.ts`), сборка через `buildContent()` вместо loader/manifest, bootstrap синхронный (`initRegistry(buildContent())`). Удалены `public/content/`, `scripts/generate-manifest.js`, `src/content/loader.ts`. Моддинг заменой файлов больше не поддерживается. Обновлены `docs/agents/CONTENT.md` и `docs/architecture/CONTENT_PIPELINE.md`. |
| 2026-07-30 | Слоистая модель клетки, фаза 5 (ловушки) реализована. Новый вид сущности `trap` (`TrapEntity`: проходимая, `hidden`, без `Attackable` и `interactionKind`): контентная категория `traps` (схема с `oneShot`/`initiallyHidden`, загрузчик, реестр, тексты ru/en, манифест, валидация `ruleIds`), слот `floorFixture` в `getPlacementSlot`. Слой `object` собирает правила ловушек независимо от `hidden`; срабатывание — правило с триггером `ENTITY_MOVED` (пример: `spikes_deal_damage`, урон вошедшему через `eventSource`). Решение в начале фазы: жизненный цикл процедурный, без нового `RuleEffect` — срабатывание правила ловушки порождает интент `DESTROY_OBJECT` (одноразовая уничтожается, событие `OBJECT_DESTROYED`) или `REVEAL_OBJECT` (постоянная раскрывается, событие `OBJECT_REVEALED`), по образцу процедурных `charges` у poi. Скрытая ловушка не рисуется и не попадает в popover вне debug-режима и не влияет на автопуть (проверено тестом: путь через клетку со скрытой ловушкой строится, срабатывание — по факту входа). Пример — колючки (`public/content/entities/traps/spikes.json`, одноразовые). Обезвреживание и поиск ловушек отложены. Новые доки: `docs/recipes/add-trap.md`; обновлены концепт (статус `[STABLE]` по фазам 1–5), план миграции (фаза 5 ✅, уточнение фазы 6 — уклонение AI от раскрытых ловушек), `docs/agents/CONTENT_RULES_EDGE_CASES.md`, `src/simulation/content-rules/AGENTS.md`. |
| 2026-07-30 | Типизация ID-параметров контента (план `docs/plans/typed-content-ids.md`, фазы 1–2). Новый файл `src/content/ids.ts` — замкнутые наборы `WEAPON_FORMULA_IDS` / `AI_STRATEGY_IDS` / `MAP_STRATEGY_IDS`; схемы подключили их через `z.enum` (`weapon.damageFormulaId`, `entities[].aiStrategyId`, `maps[].strategy`), реестры simulation типизированы от union-типов (`weapon-formulas.ts`, AI `strategy-registry.ts`, map-generation). Неиспользуемая `registerWeaponFormula` удалена (моддинг отменён). Новый модуль `src/content/validate-references.ts` — валидация перекрёстных ссылок между шаблонами (снаряжение, способности, lootTable, пулы карт, статусы и пр.), подключена в `scripts/validate-content.ts` и `src/bootstrap.ts`; негативный тест в `tests/integration/validate-content.test.ts`. Битых ссылок в текущем контенте не найдено. Обновлены `docs/agents/CONTENT.md`, `src/content/AGENTS.md`, рецепты `add-weapon.md` / `add-enemy.md` / `add-map.md`. Фаза 3 (каталог игровых тегов) остаётся отдельной задачей. |
| 2026-07-31 | Варианты спрайтов объектов по визуальному стейту. Объектные шаблоны (`stairs`/`doors`/`props`/`pois`/`traps`) получили опциональное поле `spriteVariants` (`SpriteVariantsSchema`); новый модуль `src/presentation/objectSpriteResolver.ts` — реестр `STATE_RESOLVERS` (door: `open`, poi: `depleted` при `charges === 0`) и `buildObjectSprites(state)` с приоритетом `spriteVariants[state]` → legacy `openSpriteId` → конвенция `<id>_<state>.png` (`resolveObjectSprite` в `src/utils/assetResolver.ts`). `RenderInput.doorSprites` обобщён до `objectSprites` (entityId → путь), `EntityRenderer` и `mapPoiToPopover` используют его. Ассет-заглушка `altar_depleted.png`. Обновлены `docs/recipes/add-poi.md` (раздел «Варианты спрайтов по состоянию»), `docs/agents/CONTENT.md`, `docs/architecture/DATA_FLOW.md`. |
| 2026-07-31 | Экспериментальное псевдо-3D: плоскость пола сжата по вертикали. Новые константы `FLOOR_Y_RATIO` (0.5) и `TILE_HEIGHT` (16) в `src/utils/constants.ts` — мировая Y-координата клетки считается как `y * TILE_HEIGHT` во всех рендерерах и исполнителях анимаций. Террейн получил опциональное поле `standing` (`TerrainTemplateSchema`, у `wall` = true): «стоячие» тайлы/объекты рисуются в полный размер 32×32 с привязкой низа спрайта к низу сжатой клетки; пол, слой `cover`, ловушки, туман, таргетинг и debug-оверлей следуют сжатой сетке. `DisplayTile` получил `standing?`, `TileEffectOverlay` — обязательное `layer: TileEffectLayer` (cover сжимается, aboveGround и статусы — полный размер на сжатой сетке). Обновлён раздел «Coordinate System» в `src/ui/renderer/README.md`. |
| 2026-08-02 | Создан раздел геймдизайна: `docs/game-design/mechanics-overview.md` — обзор реализованных механик (ручки настройки для контента) и перечень пробелов (xp/level, боевые роллы, спавн объектов в генераторе и пр.). Документ добавлен в `INDEX.md` и сводку `SYNC_STATUS.md` (`[STABLE]`, источник правды — код). |
| 2026-08-02 | Удалён `docs/design/starting-rules-catalog.md` (вместе с опустевшей папкой `docs/design/`) — каталог стартовых правил был тестовым и служил проверкой механик. Ссылки убраны из `INDEX.md`, `CONTENT_RULES_EDGE_CASES.md`, `post-mvp-backlog.md`, `src/simulation/content-rules/AGENTS.md` и `docs/game-design/mechanics-overview.md`; в последний добавлена пометка, что текущие правила, предметы и способности — тестовый контент для проверки механик. Записи об этом файле в истории выше оставлены как исторические. |
| 2026-08-02 | Удалены временные рабочие документы: папки `docs/plans/` (add-oil-barrel-prop, cell-layers-migration, cell-layers-prompts, typed-content-ids, post-mvp-backlog) и `docs/concepts/` (cell-layers-model, sticker-hp-outline), а также артефакты завершённого обсуждения боевой системы `docs/agents/COMBAT_SYSTEM_CONCEPT_CONTEXT.md` и `docs/agents/PROMPT_COMBAT_DISCUSSION_START.md`. Соответствующие строки убраны из сводки SYNC_STATUS; ссылки почищены в `INDEX.md`, `CONTENT_RULES_EDGE_CASES.md`, `src/simulation/content-rules/AGENTS.md` (+ комментарий в `types.ts`), `docs/recipes/add-terrain.md`, `PRESENTATION_CONTRACT.md`, `docs/game-design/mechanics-overview.md`. Источник правды по реализованным системам — код и `[STABLE]`-документы (`CONTENT_RULES_EDGE_CASES.md`, `TILE_EFFECTS.md` и пр.). Записи об этих файлах в истории выше оставлены как исторические. Невыполненные остатки планов (фаза 6 слоистой модели, фаза 3 typed-content-ids, пункты post-MVP бэклога) зафиксированы в разделе «Пробелы» `docs/game-design/mechanics-overview.md`. |
| 2026-08-03 | Утверждён концепт прогрессии внутри забега (`docs/game-design/progression-concept.md`, добавлен в сводку как `[STABLE]`): фиксированные характеристики, два слоя (экипировка + реликвии), MVP — выбор 1 из 3 на алтаре в начале этажа. `roadMap.md` актуализирован: новый «Этап 0» перед этапом 1 (включая удаление XP-задела — отдельным коммитом при реализации), этап 2 сокращён до экономики; перед этапом 1 зафиксированы 7 вопросов по механикам. Пометка `[DRAFT]` снята в `INDEX.md`. |
| 2026-08-04 | Разобраны и решены все 7 вопросов по механикам перед этапом 1 (`roadMap.md`): детерминированный бой (всегда попадание, крит по условию `dazed`/`stunned`, `chance` — опциональная механика с пометками в `src/simulation/content-rules/{types.ts,AGENTS.md,README.md}`); босс — контентом на общих методах (+ обобщённый self-buff исполнитель); `isBoss` + `bossPool` + босс-комната с запирающимися дверями и комнатой награды; генерация переведена на типы комнат (стартовый набор `start`/`normal`/`elite`/`boss`/`reward`); слияние стопок перенесено в этап 1.4; состав расходников — при дизайне п. 1.4; лечение — дефицитный ресурс (MVP: алтарь + редкие расходники). В `roadMap.md` добавлен раздел «Отложенные доработки» с привязкой к этапам. Исправлен рассинхрон в `mechanics-overview.md` §10.4 (условия работают с `modifyDamage`, подтверждено кодом и тестом). |
| 2026-08-05 | Выполнен этап 0.1 roadmap (фаза A плана `docs/plans/stage-0-relics-altar.md`): удалён XP-задел — поля `xp/level` в `PlayerEntity` и `PlayerStatsSnapshot`, `xpReward` из `EntityTemplateSchema` и шаблонов котов, константа `XP_PER_LEVEL`, событие `PLAYER_LEVELED_UP` (+ патч presentation и case в `fogFilter`), `source: 'levelup'` у способностей, xp-бар и бейдж уровня в UI (`HeroPanel`, `Portrait`, `ResourceBar`, стили), i18n-ключи `common.game.xp/level`, `heroPanel.xpLabel`, `skillsPanel.levelupSkillTooltip`, namespace `portrait` (был из одного `levelAriaLabel`), ассет `icons/xp.svg`. Актуализированы `mechanics-overview.md`, `docs/recipes/add-enemy.md`, `docs/architecture/CONTENT_PIPELINE.md`, `docs/agents/I18N.md`. Уровни способностей (`grantedAbilities[].level`, `RuntimeAbility.level`) не затронуты. |
| 2026-08-05 | Выполнен этап 0.4 roadmap (фаза C плана `docs/plans/stage-0-relics-altar.md`): генератор этажей размещает гарантированный poi стартовой комнаты — `MapParams.startPoiId` (временная мера до типов комнат этапа 1), `GeneratedMap.pois`, `spawnStartPoi` в `tree-room-strategy` (8-соседняя со спавном клетка корневой комнаты, `state.rng`), poi включён в сущности этажа в `simulation.ts` и `floor-transition-planner.ts`. Новый шаблон poi `relic_altar` (тексты ru/en, спрайты-заглушки, `depleted` по конвенции) — перенесён из фазы D; случайного спавна poi из пула нет. Валидация `startPoiId → pois` в `validate-references.ts`. Обновлены `mechanics-overview.md` (§6.4, §10.2) и примечание по `progression-concept.md` в сводке выше. |
| 2026-08-05 | Выполнен этап 0.3 roadmap (фаза E плана `docs/plans/stage-0-relics-altar.md`) — панель коллекции реликвий: `RelicViewModel` + `RenderInput.relics` (группировка по шаблонам, порядок получения), `RelicsPanel`/`RelicDetailPopover` (горизонтальный скролл, поповер с описанием и flavorText), `flavorText` у всех 8 реликвий (ru/en) и в `LocalizedRelicTemplate`, i18n `components.relicsPanel*`. Фазы D (алтарь выбора) и F (стартовый пул) при этом оказались реализованы ранее — статусы в таблице плана приведены в соответствие коду. Обновлены `mechanics-overview.md` (§9) и примечание по `progression-concept.md` в сводке выше: этап 0 выполнен целиком. |
| 2026-08-05 | Доработано отображение реликвий (модалка алтаря + тултип коллекции): монолитное `description` реликвий удалено — у реликвии в `texts/{ru,en}/relics.ts` остались `name` + `flavorText`, механика переехала в тексты правил (15 новых записей в `texts/{ru,en}/rules.ts` для реликвийных ruleIds, сверены с `CONTENT_RULES`). `LocalizedRelicTemplate` больше не несёт `description`. Presentation: `RelicEffectViewModel` + билдер `buildRelicEffects` (правила + statModifiers с локализованными именами статов — новая i18n-группа `system.statNames`, формат «+N»/«−N»/«×N»); `RelicViewModel.effects` вместо `description`; `PendingWindowViewModel.options` — выделенный `RelicChoiceOptionViewModel`. UI: `RelicDetailPopover` и `RelicChoiceModal` показывают эффекты списком «имя + описание» через `RichDescription`, flavorText курсивом внизу. Актуализирован рецепт `docs/recipes/add-relic.md`: механика реликвии обязана жить в текстах правил. |
| 2026-08-06 | Финальное ревью этапа 0 (план `docs/plans/stage-0-relics-altar.md`): исправлены 3 major-бага реликвий — `relic_thunderhead_daze` дезил владельца при ударе по нему (добавлен `eventRole: 'source'`, заодно в `relic_scavenger_heal_on_pickup`); протухший `offer` алтаря молча тратил 1 AP (новый опциональный `canResolve` в `PoiWindowMechanic` + проверка в validate `RESOLVE_POI_CHOICE`); `refreshPendingWindow` переоткрывал окно после любого dispatch — отказ не работал (окно открывается только по факту активации poi через `windowCandidatePoiId`). Заодно `debugRegenerateMap` сбрасывает окно (soft-lock, dev-only). Pre-existing без фикса: тот же изъян `eventRole` у `weapon_blunt_daze` (смягчён `chance: 25`); поэтажные `relicPool` — мёртвый контент (`mapParams` фиксируются на floor_1). Этап 0 отмечен выполненным в `roadMap.md`. Обновлены `docs/recipes/add-relic.md`, `docs/recipes/add-poi.md`, журнал плана. |
| 2026-08-06 | Окно выбора реликвий алтаря (`RelicChoiceModal`) приведено к виду «3 карточки в ряд»: карточка содержит только спрайт (рамка редкости + иконка, как ячейка коллекции) и имя; описание (эффекты + flavorText) всплывает по наведению через переиспользованный `RelicDetailPopover`. Presentation: `RelicChoiceOptionViewModel` получил `frameUrl` (через `resolveItemFrame`). CSS: карточки `cm-choice-card` — по 3 в ряд, мёртвые стили инлайн-описаний (`__head`, `__effects`, `__flavor` и пр.) удалены из `runtime.css`. Актуализирован рецепт `docs/recipes/add-relic.md` (flavorText — в тултипе, не в карточке). |
| 2026-08-06 | Унификация размещения спрайтов (SpritePlacement). Единый механизм: `SpritePlacementSchema` (опциональное поле `placement` в шаблонах сущностей/игрока/объектов/эффектов/статусов), резолвер дефолтов категорий `src/presentation/spritePlacementResolver.ts` (`getSpritePlacement`), единая геометрия `src/ui/renderer/spritePlacement.ts` (`applyPlacement`, `cellRect`, `cellCenter`). Константы `STANDING_Y_FACTOR`, `TILE_EFFECT_STATUS_SPRITE_SCALE`, `TILE_EFFECT_STATUS_OFFSET_Y_FACTOR` удалены (значения стали дефолтами категорий, визуально ничего не изменилось); `renderScaleResolver.ts` удалён (поглощён резолвером, `renderScale` мапится на `placement.scale`). Рендереры (`EntityRenderer`, `TileEffectRenderer`, `TileRenderer`, стратегии статусов), туман, таргетинг, debug-оверлей, камера и исполнители анимаций переведены на новый модуль. Заодно исправлен рассинхрон: в `src/ui/renderer/README.md` заявлялось `FLOOR_Y_RATIO = 0.5`/`TILE_HEIGHT = 16` — актуальные значения 0.9/28.8. Обновлены `docs/agents/CONTENT.md`, `docs/agents/PRESENTATION_CONTRACT.md` (§2.7), рецепты `add-enemy.md`/`add-poi.md`/`add-prop.md`/`add-trap.md`/`add-tile-effect.md`, `src/presentation/AGENTS.md`, `src/ui/renderer/README.md`. |
| 2026-08-06 | Удалено поле `renderScale` из схем шаблонов (Entity/Player/Stairs/Door/Prop/Poi/Trap) — дублировало `placement.scale`. Шаблоны мигрированы: значения 1 просто удалены (дефолт масштаба всех категорий теперь 1.0, включая `actor` — прежний дефолт 1.5 игрока был мёртвым кодом, все шаблоны игроков задавали 1), значения ≠1 (`cat-small` 0.8, `cat-guardian` 1.2, `cat-big` 0.9) перенесены в `placement: {scale: N}`. Резолвер больше не читает `renderScale`; тестовые моки и рецепты обновлены. Визуально ничего не изменилось. |
| 2026-08-06 | Тултип реликвии упрощён до чистых эффектов (решение пользователя): имена правил больше не показываются — пункт правила содержит только описание; модификаторы характеристик выводятся одной строкой «Имя: +N» (имя из `system.statNames`, значение «+N»/«−N»/«×N»). `RelicEffectViewModel` — `{key, text}` вместо `{key, name, description}`; `buildRelicEffects` собирает строки на месте. `RelicDetailPopover` рендерит каждый эффект одной строкой через `RichDescription`, стили `field-popover-effect__name`/`__desc` удалены (свойства `__desc` перенесены на `.field-popover-effect`). Обновлены `relicEffects.test.ts` и рецепт `docs/recipes/add-relic.md`. |
| 2026-08-07 | Зачистка боевых роллов + детерминированный крит (план `docs/plans/combat-rolls-cleanup.md`, roadmap вопрос №1). Фаза A: удалены статы `accuracy`/`dodgeChance`/`critChance` и событие `ENTITY_MISSED` по всему коду (типы, дефолты, резолверы, снапшоты, схема контента, presentation, i18n, тесты); `critMultiplier` сохранён. Фазы B–C: крит — глобальное правило `core_crit_on_dazed_stunned` (урон ×`critMultiplier` атакующего по цели в `dazed`/`stunned`, читается из derived-кэша через новое поле контекста `sourceCritMultiplier` в DSL), индикация — строка лога `damageTakenCrit` и floating text «Крит!» по тегу `crit`. Фаза D: `weapon_poison_on_hit` и `weapon_blunt_daze` переведены с `chance` на «всегда», `weapon_blunt_daze` получил `eventRole: 'source'` (закрыт pre-existing изъян самодеза из ревью этапа 0). Обновлены `mechanics-overview.md` (§1.3, §1.4, §10.1, §11), `roadMap.md`. Баланс значений/длительностей яда и деза — отдельный проход в этапе 1.4. |
| 2026-08-07 | Отвязка тестов от контентных данных (план `docs/plans/decouple-tests-from-content.md`). Тесты механик переведены на синтетические фикстуры: `relicViewModel.test.ts` и `relicEffects.test.ts` — на мок-реестр и подмену `texts/lookup`, `relic-rules.test.ts` — на тестовые правила с числами из фикстур (удалён describe на реальном контенте), `relic-registry.test.ts` — без describe на реальном контенте, tile-effects integration (`oil-burning-cycle`, `ability-sources-cycle`, `burning`) — на новую фикстуру `tests/fixtures/tile-effects.ts` (синтетические шаблоны тайловых эффектов, статусов и правила). `cat-guardian-template.test.ts` — только структурная Zod-валидация (data-asserts удалены). В `TESTING.md` добавлен принцип «независимость от балансных данных контента». |
| 2026-08-08 | Экипировка: подтипы, уровни, аффиксы + урон рейнжем (концепт `docs/game-design/equipment-modifiers-concept.md`, реализация завершена). Новая контентная категория `modifiers` (4 шаблона аффиксов, тексты ru/en, валидация); у шаблонов экипировки — `subtype`/`level`, у оружия `damage {min,max}` вместо `baseDamage`; формулы урона оружия удалены (`weapon-formulas.ts`, `damageFormulaId`, `WEAPON_FORMULA_IDS`); урон роллится в момент удара со смещением от dex (`rollWeaponDamage`, `DEX_DAMAGE_BIAS_K`); аффиксы экземпляра — 1 положительный + до 1 отрицательного (`rollItemAffixes`, `NEGATIVE_AFFIX_CHANCE`), `ParametrizedValue` +`ownerParam` / `ActiveRule.paramValue`; presentation/UI показывают рейнжи и аффиксы. ~104 файла. Обновлены `CONTENT.md`, `src/content/AGENTS.md`, `src/simulation/AGENTS.md`, `CONTENT_RULES_EDGE_CASES.md`, `mechanics-overview.md`, `progression-concept.md`, рецепты `add-weapon.md`/`add-modifier.md` (новый), `INDEX.md`, `GLOSSARY.md`, `SYNC_STATUS.md`. |
| 2026-08-08 | Ревью-правки системы аффиксов (по итогам код-ревью реализации). Удалено мёртвое поле `combat` из `EntityTemplateSchema` и инициализации `createEnemy` (урон/броня врага всегда пересчитывались из шаблонов экипировки); `formatDamageRange` унифицирован в `src/utils/format.ts` (удалён `src/ui/format.ts` и дубль в `itemDetailMapper`); из `registry.ts` убран неиспользуемый API (`getModifier`/`getLocalizedModifier`/`getAllModifiers`/`LocalizedModifierTemplate`), из `validate-references.ts` — недостижимая проверка `applicableSubtypes` (покрыта `z.enum`). Новые семантические валидации: stat-аффикс требует `scaling: perLevel` (`validateContentRuleSemantics`), `{value}` в описании модификатора — только при `perLevel` (`validateModifierTextPlaceholders` в `validate-references.ts`, вызывается из `scripts/validate-content.ts`). Тесты: +5 в `validation.test.ts`, +2 в `validate-content.test.ts`. Обновлены `CONTENT.md`, `CONTENT_PIPELINE.md`, `mechanics-overview.md`, `add-modifier.md`.
| 2026-08-09 | Унификация свойств экипировки через модификаторы: у шаблонов экипировки `fixedModifiers` вместо `equipModifiers`/`ruleIds`; у `ItemAffix` — `origin: 'fixed' | 'rolled'` (единый список: фирменные + до 2 случайных); у модификаторов — `poolEligible` (default true) и `scaling fixed`, `polarity` получила default 'positive'; ролл фильтруется по `poolEligible`/`applicableSubtypes` и исключает фирменные модификаторы и конфликтующие ruleId (дубль `mod_poison_on_hit` на стартовом мече устранён); рантайм применяет stat-аффиксы и правила единым проходом из `item.affixes`; в UI — единая секция «Свойства» с пометкой ✦ у случайных. Контент: 6 новых фирменных модификаторов (`poolEligible: false`), `mod_poison_on_hit` — фирменный у common_splinter_blade/common_venom_dagger и в пуле ролла. Обновлены `equipment-modifiers-concept.md`, `CONTENT.md`, `src/content/AGENTS.md`, `src/simulation/AGENTS.md`, `mechanics-overview.md`, рецепты `add-modifier.md`/`add-weapon.md`, `GLOSSARY.md`, `src/simulation/content-rules/AGENTS.md`, `SYNC_STATUS.md`. |
| 2026-08-09 | Переработана карточка предмета (`ItemDetailCard`): фиксированный порядок категорий без заголовков — статы (значения жирным) → уникальные свойства → случайные свойства → скиллы (установленные + пул возможных с пометкой «возможный») → флейвор-описание курсивом → теги; категории разделяются полосой `item-detail-divider`. У модификаторов в `ItemDetailViewModel.properties` появилось поле `polarity`; подсветка: `item-detail-property--unique` (фирменные) и `--positive`/`--negative` (случайные, по полярности). Удалены заголовки секций и пометка ✦, мёртвый вариант секции `description` из `ItemDetailSection`, i18n-ключи `itemDetail.itemSkillsTitle/possibleSkillsTitle/descriptionTitle/propertiesTitle/rolledPropertyHint`; добавлен `itemDetail.possibleSkillHint`. Обновлён `equipment-modifiers-concept.md` (§7). |
| 2026-08-09 | Исправлен баг derived-кэша характеристик: `recalculateActorStats` записывал `maxHp`/`armor`/`damage` из чистой базы, игнорируя stat-модификаторы. Модификаторы maxHp (`mod_fragile`, `mod_guardian_vitality`, `relic_scavenger`) не действовали вовсе (`getEffectiveMaxHp` нигде не вызывался), кэш брони/урона расходился с боевым расчётом и UI. Кэш теперь хранит итоговые значения (база + модификаторы), как `critMultiplier`. Добавлены тесты в `tests/unit/simulation/systems/stats/stats.test.ts`; уточнён `mechanics-overview.md` (§1.4). |
| 2026-08-09 | Цветовое выделение эффектов реликвий по полярности (по образцу свойств карточки экипировки). `ContentRule` получил опциональное `polarity` ('positive' по умолчанию; `'negative'` проставлена у 6 негативных реликвийных правил); `RelicEffectViewModel` — поле `polarity`: для правил из определения, для `statModifiers` — из знака (`add < 0`/`multiply < 1` → negative). `RelicDetailPopover` (коллекция и окно выбора алтаря) выделяет пункты классами `field-popover-effect--positive/--negative` (цвет текста и фон, как у свойств экипировки). Обновлены `relicEffects.test.ts` и рецепт `add-relic.md`. |
| 2026-08-10 | `roadMap.md` очищен от выполненного: удалён этап 0 (реликвии и алтарь выбора, выполнен 2026-08-06) и раздел «Вопросы по механикам» (все 7 решены 2026-08-04, вопрос №1 реализован 2026-08-07) — актуальные решения перенесены в пункты этапов 1.2 (типы комнат), 1.3 (босс как контент, босс-комната, запирающиеся двери), 1.4 (расходники, экономика лечения, слияние стопок, баланс яда/деза) и 2.3; «Отложенные доработки» сохранены. Актуализированы строки про контент-пайплайн (TypeScript вместо JSON) и боевую систему в «Что уже есть». |
| 2026-08-10 | Утверждён концепт первого босса: новый документ `docs/game-design/first-boss-concept.md` (способности Глухая оборона / Налёт / Удар по земле, две стадии с переходом на 50% HP, контрплей, объём доработок движка), добавлен в сводку как `[STABLE]` и в карту `INDEX.md`. В `roadMap.md` пункт 1.3 ссылается на концепт; отложенный вопрос о варианте щита закрыт (периодическая неуязвимость), добавлена отложенная доработка «немедленная реакция перехода стадий». |
| 2026-08-10 | Составлен план реализации первого босса (`docs/plans/first-boss-implementation.md`, 9 шагов). Пол «минимум 1 урона» снят из расчёта урона (`damage-handlers.ts`, решение пользователя): броня может обнулить физический урон; граница 1 сохранена только для процентного урона (`min: 1` в правилах тиков). Обновлены 4 теста; концепт босса (реализация иммунитета — движковое обнуление в `applyDamageToEntity`), `mechanics-overview.md` (§1.3) и `roadMap.md` (вопрос семантики multiply 0 снят) приведены в соответствие. |
| 2026-08-10 | План первого босса, шаги 1–3 выполнены. Статус `bulwark` (шаблон, тексты, иконка-заглушка, сортировка в UI); движковая семантика: иммунитет к урону через обнуление в `applyDamageToEntity` (событие с damage 0 эмитится — статусы накладываются), гашение PUSH, запрет действий носителя в `canActorAct` без сброса подготовки (хелпер `isBulwarked`); self-buff: поле `selfBuff` в `AbilityTemplateSchema`, фабрика `createSelfBuffSkill` + fallback в `getSkillExecutor`, способность `bulwark` (apCost 1, cooldown 4, статус на 1 ход). Тесты: unit `bulwark.test.ts`/`selfBuffSkill.test.ts` + интеграция `bulwark-scenario.test.ts`. Обновлены `src/simulation/AGENTS.md`, `src/content/AGENTS.md`, `docs/recipes/add-ability.md`, журнал плана. |
| 2026-08-11 | Шаблоны способностей — discriminated union `kind` (план `docs/plans/ability-templates-kind-union.md`, выполнен целиком; закрывает шаг 4 плана первого босса). `AbilityTemplateSchema` → union по `kind`: общая база (метаданные, `damageTag?`, `requiredWeaponTags`) + параметризованные виды `selfBuff` (`statusType`, `duration`) и `swoop` (`jumpRadius`/`aoeRadius`/`baseDamage`) + 6 legacy-видов без параметров (`fireball`, `magicSlap`, `dash`, `counterattack`, `cleave`, `suddenStrike`). Разрешение исполнителя в `getSkillExecutor`: фабрика по kind (`KIND_FACTORIES`: `createSelfBuffSkill`, `createSwoopSkill`) → кэш → legacy-реестр по id; регистрация swoop из `skills/index.ts` удалена, константы/инстанс swoopSkill удалены. Новый контент: `guardian_swoop` («Налёт» босса, 3/1/10 черновик, apCost 2, cooldown 2, aiPreparable) + тексты ru/en + переиспользованный `swoopComposer`. Валидация `statusType` self-buff в `validate-references.ts`. Моки тестов (~20 файлов) мигрированы на union. Обновлены `src/content/AGENTS.md` (раздел «Способности»), `src/simulation/AGENTS.md` (разрешение исполнителей), `docs/agents/CONTENT.md` (новый раздел «Шаблоны способностей (union kind)»), `docs/recipes/add-ability.md` (выбор kind, параметры вида), `first-boss-implementation.md` (решение C и шаг 4 заменены ссылкой на план union). |
| 2026-08-11 | `counterattack` мигрирован в вид `kind: 'selfBuff'` (по плану `ability-templates-kind-union.md` — кандидат из рисков): шаблон получил `statusType: 'counterattack'`, `duration: 2` (длительность стала контентным параметром); `counterattackSkill.ts` и регистрация в `skills/index.ts` удалены, член union `counterattack` убран — legacy осталось 5 видов (`fireball`, `magicSlap`, `dash`, `cleave`, `suddenStrike`). Поведение без изменений (фабрика `createSelfBuffSkill` написана по образцу удалённого исполнителя; сама реакция контратаки — контентные правила, от исполнителя не зависит). `counterattack.test.ts` переведён на `getSkillExecutor`/фабрику. Обновлены `src/content/AGENTS.md`, `src/simulation/AGENTS.md`, `docs/agents/CONTENT.md`, `docs/recipes/add-ability.md`, журнал плана union. |
| 2026-08-11 | План первого босса, шаг 5 выполнен («Удар по земле» + правило dazed): новый параметризованный вид union `kind: 'groundSlam'` (`radius`/`baseDamage` в шаблоне) + фабрика `createGroundSlamSkill` в `KIND_FACTORIES` (TargetMode self, зона (2·radius+1)² от позиции кастера, DAMAGE всем damageable кроме кастера с тегом идентичности `skill.<id>`); формула `ground_slam` (flat blunt) в `damageFormula.ts`; правило `ground_slam_daze` (`content-rules/ground-slam-rules.ts`, триггер `ENTITY_DAMAGED` с тегом `skill.ground_slam`, `eventRole: 'source'`, dazed 2 по `eventTarget`) в `CONTENT_RULES`; шаблон `ground_slam` (radius 2, baseDamage 12 черновик, apCost 2, cooldown 4, aiPreparable) + тексты ru/en + ассет-заглушка `skills/ground_slam.png`. Телеграф зоны — generic (`getAffectedPositions`), presentation не менялся. Тесты: unit `groundSlam.test.ts` (6) + интеграция `ground-slam-scenario.test.ts` (3); полный прогон зелёный (1574 теста). Обновлены `src/content/AGENTS.md`, `src/simulation/AGENTS.md`, `docs/agents/CONTENT.md`, `docs/recipes/add-ability.md`, журнал плана `first-boss-implementation.md`. |
| 2026-08-11 | План первого босса выполнен целиком (шаги 6–9). Тактика `findCollisionLanding` (`src/simulation/ai/tactics/ability.ts`): выбор точки применения способности со столкновением цели (порядок — расстояние/x/y, цель в зоне через `getAffectedPositions`, препятствие за целью по семантике `executePushIntent`). Стратегия `guardian-boss` (`src/simulation/ai/guardian-boss-strategy.ts`, id в `AI_STRATEGY_IDS`): охотничий FSM + две стадии (порог 50% HP, опциональные поля `bossStage`/`bossTransitionPending` в `AIState` — сейвы не ломаются), комбо «Удар + Оборона» с приоритетом над Налётом, эвристика «конца хода» `AP ≤ 1 || hunterAction === END_TURN`. Hunter FSM-хелперы и `decideHunterAction` вынесены из `hunter-strategy.ts` в `ai-helpers.ts` (поведение hunter не изменилось). Контент `cat_guardian`: способности `guardian_swoop`/`ground_slam`/`bulwark`, `aiStrategyId: 'guardian-boss'`, HP 80 → 90 (черновик). Тесты: unit `tactics/ability.test.ts` (8), `guardian-boss-strategy.test.ts` (13), интеграция `guardian-boss-scenario.test.ts` (5); полный прогон зелёный (1600 тестов), validate:content и typecheck чисты. Обновлены `docs/agents/AI_SYSTEM.md`, `docs/game-design/mechanics-overview.md`, `roadMap.md`, журнал плана. |
| 2026-08-12 | Добавлен глобальный скейлер скорости всех игровых анимаций `ANIMATION_SPEED_SCALE` (по умолчанию 1): определён в `src/utils/tween.ts`, применяется в `Tween.update` (покрывает все твины, включая захардкоженные длительности dash/meteor flash), реэкспортирован из `src/utils/animationConfig.ts`; стаггер всплывающего текста (`STAGGER_MS` в `pixiFloatingTextExecutor.ts`) скейлится тем же множителем. CSS-анимации и тосты (UI-хром) сознательно не затронуты. Обновлён `src/ui/AGENTS.md`. |
| 2026-08-12 | Индикация крита объединена с числом урона: отдельный floating text «Крит!» убран — критический урон отображается как «N!» (`PixiFloatingTextExecutor` добавляет «!» к числу урона при теге `crit` в DAMAGE-шаге); `entityDamagedBuilder` больше не создаёт отдельный UI_FLOATING_TEXT-узел; i18n-ключ `system.animation.crit` удалён из схемы и локалей ru/en. Обновлён `mechanics-overview.md` (§1.3). |
| 2026-08-12 | Крит ограничен уроном оружия: триггер правила `core_crit_on_dazed_stunned` получил фильтр `tags: [\x27delivery.weapon\x27]` — критуют базовые атаки и оружейные скиллы (cleave/suddenStrike), урон способностей (fireball, ground slam), тиков статусов и столкновений по dazed/stunned-цели не умножается. Тесты: в `crit-on-dazed-scenario.test.ts` добавлен негативный кейс (fireball по dazed-цели без тега `crit`), фикстура `test_blade` получила теги оружия. Обновлён `mechanics-overview.md` (§1.3). |
| 2026-08-12 | Составлен план миграции оставшихся 5 legacy-видов шаблонов способностей (`docs/plans/ability-templates-legacy-migration.md`, продолжение `ability-templates-kind-union.md`). Утверждено: урон способностей — фиксированные значения из шаблона (формулы со скейлингом `int`/`str`/`skillLevel` удаляются, модификаторы урона — через стандартные модификаторы и правила), `cleave` — фабрикой без параметров, стейджинг по видам, финал — удаление legacy-реестра исполнителей. |
| 2026-08-12 | План `ability-templates-legacy-migration.md` выполнен целиком. Все виды union `kind` получили фабрики: `fireball` (`range`/`aoeRadius`/`centerDamage`/`aoeDamage` 5/1/20/10), `magicSlap` (`range`/`targetCount`/`baseDamage` 5/3/12), `dash` (`distance`/`bumpDamage` 2/5), `suddenStrike` (`silenceDuration` 2), `cleave` (без параметров). Урон способностей — фиксированные значения из шаблона (swoop/groundSlam тоже переведены на плоский урон); `damageFormula.ts` удалён целиком (формулы со скейлингом ушли, `push_bump` оказалась мёртвым кодом, `ground_slam` заинлайнена в фабрику). Legacy-реестр исполнителей удалён (`registerSkill`/`initSkillRegistry`, legacy-ветка `getSkillExecutor`, вызов в `defaultActionHandlerRegistry`); `KIND_FACTORIES` — полный `Record` по всем kind; `skills/index.ts` — только реэкспорт. Тесты: моки ~30 файлов, `tests/helpers/test-skills.ts` удалён, `required-weapon-tags.test.ts` на вид `selfBuff`. Прогоны зелёные (полный vitest 1588 passed; 13 pre-existing падений в `tests/unit/ui/**` и tween — изъян коммита со скейлером скорости анимаций, вне области). Обновлены `src/content/AGENTS.md`, `src/simulation/AGENTS.md`, `docs/agents/CONTENT.md`, `docs/recipes/add-ability.md` (legacy-путь удалён), `docs/recipes/add-tile-effect.md`, `mechanics-overview.md`, `equipment-modifiers-concept.md`. |
| 2026-08-12 | Стиль всплывающих цифр урона (визуальная доработка, слой UI): `FloatingTextRenderer` получил чёрную обводку текста (как у цифр ожидаемого урона в `TargetingRenderer.drawDamageNumber`), убран fade-out (текст только поднимается, не растворяется); крит отображается крупнее — опциональный `sizeScale` в `FloatingTextRenderer.show`/`WorldRenderer.showFloatingText`, константа `CRIT_SIZE_SCALE = 1.25` в `pixiFloatingTextExecutor.ts` (по тегу `crit`). Typecheck чист; падения в `tests/unit/ui/**` — те же pre-existing (скейлер скорости анимаций). Обновлён `mechanics-overview.md` (§1.3). |
| 2026-08-12 | Знаки у всплывающих чисел: урон отображается со знаком минус («-N», крит — «-N!»), лечение — со знаком плюс («+N», было ранее в `healNode`). Изменение точечное в `pixiFloatingTextExecutor.ts`. Обновлён `mechanics-overview.md` (§1.3). |
| 2026-08-12 | Стиль подсветки таргетинга и автопути (`TargetingRenderer`, визуальная доработка, слой UI). Подсветка клеток унифицирована: рамка толщиной 2 экранных пикселя с отступом 3 пикселя внутрь клетки + полупрозрачная заливка тем же цветом (`drawCellHighlight`, заменила `drawOverlay`/`drawTileOutline`); мёртвые `ALPHAS.pathPreview/pathMove/pathEnemy` заменены на `ALPHAS.pathTarget`. Толщина всех рамок и линий задана в экранных пикселях и не масштабируется zoom'ом (мировые значения делятся на zoom: рамки, линия пути, отметки концов ходов, стрелки превью интентов). Линия автопути — пунктир 2px (`drawDashedLine`, штрих/промежуток 6/4 px, паттерн не сбрасывается на изломах). Новое: при наведении скиллом на валидную для каста клетку рисуется пунктирная линия 2px от персонажа до цели (цвет hover). Тесты: +3 в `TargetingRenderer.test.ts`; typecheck чист. Падения в `tests/unit/ui/renderer/{EntityRenderer,WorldRenderer}.test.ts` — те же pre-existing (скейлер скорости анимаций). |
| 2026-08-12 | Исправлен баг `drawDashedLine` (`TargetingRenderer`): единичный вектор направления сегмента вычислялся как `dx/dist` с уменьшающимся в цикле `dist` — штрихи накапливали ошибку и линия улетала далеко за конечную точку (тем сильнее, чем мельче штрих в мировых единицах, т.е. чем больше zoom). Направление теперь вычисляется один раз до цикла. Регрессионный тест: пунктирная линия заканчивается ровно в центре целевой клетки и не выходит за пределы сегмента. |
| 2026-08-12 | Прозрачности подсветки в `TargetingRenderer` вынесены в константы и унифицированы: `CELL_FILL_ALPHA = 0.15` (заливка всех видов подсветки клеток, включая AI-подготовленные — было 0.25), `CELL_FRAME_ALPHA = 1` (рамки), `LINE_ALPHA = 0.8` (пунктирные линии, отметки концов ходов, стрелки превью; линия автопути была 0.6). Объект `ALPHAS` удалён, параметры прозрачности убраны из сигнатур `drawCellHighlight`/`drawDashedLine`. |
| 2026-08-12 | Линия автопути следует за спрайтом персонажа во время анимации перемещения (раньше старт линии «прыгал» по центрам логических клеток: линия запекалась в Graphics на момент `render()`, а спрайт двигался по тикеру). `EntityRenderer.getVisualCenter(id)` — визуальный центр клетки сущности: текущая опорная точка спрайта переводится в центр сжатой сетки (смещение anchor→center постоянно, учитывает `anchorX/anchorY/flattenY`). `TargetingRenderer.update(input, playerVisualCenter?)` — старт линий от персонажа (автопуть и линия к цели скилла) от визуальной позиции; линия автопути хранится как персистентный Graphics (`pathLine`) и перерисовывается покадрово через `updatePathStart(center)` из `WorldRenderer.onTick` (перерисовка в том же Graphics через `clear()`, пропуск при неизменной позиции). Отрисовка пунктира вынесена в `strokeDashed(g, …)` (без создания Graphics). Тесты: +1 в `TargetingRenderer.test.ts` (старт следует за `updatePathStart`), +2 в `EntityRenderer.test.ts` (`getVisualCenter`); typecheck чист. Pre-existing падения (скейлер скорости анимаций) без изменений. |
| 2026-08-12 | Исправлен баг отметок концов ходов автопути при нулевых AP: `computeTurnEndIndices` (`gameSession.ts`) начинал расчёт с `remaining = ap` и при `ap === 0` сразу возвращал пустой список — рисочки исчезали до начала следующего хода. Теперь при нулевых AP отметки считаются от `maxAp` (показывают, до куда хватит очков следующего хода). Тест `turn end indices stop before door when only enough AP to open it` фиксировал этот баг (ожидал `[]` в окне `ap === 0` после открытия двери) — обновлён до корректного `[1]` и переименован; добавлен тест «отметки от следующего хода при ap=0» (путь 3 шага, maxAp=2 → `[1]`). Весь пакет `tests/unit/presentation` зелёный (337 тестов). |
| 2026-08-12 | Исправлен баг двойного тика урона ядом: `status_poison_tick_damage` (правило статуса `poisoned`) подхватывалось слоем `radius` от соседнего отравленного актора и наносило урон тикающей сущности повторно. Правило получило условие `eventRole: 'target'` (по образцу `weapon_blunt_daze`). Регрессионный тест в `tests/unit/simulation/status-effects/tick-phases.test.ts`; предупреждение о ловушке слоя `radius` добавлено в `docs/agents/CONTENT_RULES_EDGE_CASES.md` и `src/simulation/content-rules/AGENTS.md`. |
| 2026-08-13 | Удалены выполненные целиком планы из `docs/plans/`: `stage-0-relics-altar` (этап 0, все фазы A–F + финал), `first-boss-implementation` (все 9 шагов), `ability-templates-kind-union` и `ability-templates-legacy-migration` (union `kind` шаблонов способностей, выполнены целиком), `combat-rolls-cleanup` (фазы A–E), `decouple-tests-from-content` (фазы 1–4). Папка `docs/plans/` опустела. Ссылки почищены в `roadMap.md` (этап 0, статус 1.3, принцип self-buff) и сводке SYNC_STATUS выше; записи об этих файлах в истории оставлены как исторические. Источник правды по реализованным системам — код и `[STABLE]`-документы. Невыполненный остаток (мёртвые поэтажные `relicPool`) зафиксирован в разделе «Пробелы» `docs/game-design/mechanics-overview.md`. |
| 2026-08-13 | Создан концепт контента первого этажа `docs/game-design/floor-1-content-concept.md` (`[DRAFT]`): две линии сборки (Подвижный боец — мечи/кровотечение/рывки, Алхимик — праща/расходники/окружение), тема «подземелье замка», решения по муке (временный эффект, `blocksLOS` + сокрытие сущностей дальше 1 клетки), мышеловке (кровотечение + `rooted`, активный поиск ловушек за AP), старту (2 шаблона персонажа, выбор реликвии при создании, алтарь из стартовой комнаты убирается) и экипировке MVP (мечи/праща, тяжёлая/лёгкая броня, амулет «Тусклая бусина»). Документ добавлен в `INDEX.md` и сводку выше. Подвопросы закрыты тем же днём: негативный аффикс бусины — фича, радиус поиска — 3 клетки, пул врагов MVP — только `cat_small`/`cat_mid`; отложена механика «Броска бомбы». |
| 2026-08-14 | Реализован план `docs/plans/room-types-generation.md` (roadMap 1.2, концепт этажа 1 п.1 §3): генератор этажей переведён на **типы комнат**. Новая контентная категория `roomTypes` (`RoomTypeTemplateSchema` — union по `kind`, пока только `generated`; вид `preset` для босс-комнат задокументирован, реализация — 1.3). Тип комнаты задаёт `weight`/`minDepth`/`maxPerFloor`, `minSize`/`maxSize` и `fill`: взвешенные пулы врагов/предметов/пропов/ловушек/тайловых эффектов с плотностями от площади комнаты (площадь/16 × density) + `guaranteedPois`. Из `MapParams` удалены `enemyPool`/`itemPool`/`enemyDensity`/`itemDensity`/`minRoomSize`/`maxRoomSize`/`startPoiId`; добавлены `roomTypePool` (weighted, min 1) и `startRoomTypeId`. Шаблоны: `start` (алтарь реликвии), `normal` (этаж 1: cat_small/mid, зелья, бочки, колючки, лужи масла/воды), `normal_deep` (этаж 2). Генератор: `Room.roomTypeId`, назначение типов узлам дерева, `fillRooms` в `map-generation/shared.ts` (поглотила `spawnEnemiesAndItems`/`spawnStartPoi`), `GeneratedMap` += `props`/`traps`/`tileEffects`. Новый рецепт `docs/recipes/add-room-type.md`; обновлены `add-map.md`, `add-poi.md`, `CONTENT.md`, `src/content/AGENTS.md`, `mechanics-overview.md` (§6.4, §10.2), `roadMap.md` 1.2, концепт этажа. Проверки: typecheck/validate:content чисты; vitest — 1603 зелёных, 13 падений — pre-existing (скейлер скорости анимаций, 2026-08-12). |
| 2026-08-14 | Реализован план `docs/plans/boss-fight-infrastructure.md` (roadMap 1.3, этапы 1–7) — **инфраструктура босс-файта**. Контент: `isBoss` в `EntityTemplateSchema` (`BOSS_TEMPLATE_IDS` удалён, `isBossTemplate` читает реестр; `cat_guardian` += `isBoss`); `bossPool`/`bossRoomTypeId`/`rewardRoomTypeId` в `MapParamsSchema` (валидация — только при заданном `bossPool`, шаблоны пула обязаны иметь `isBoss: true`); `indestructible` в `DoorTemplateSchema` (движковое обнуление урона по образцу bulwark); шаблон `boss_door` (тег `boss_room`, негорючая) и типы комнат `boss`/`reward` (weight 0, reward с `guaranteedPois: ['altar']`); `floor_1` += `bossPool: ['cat_guardian']`. Движок: `DoorEntity.isLocked` + интенты `LOCK_DOOR`/`UNLOCK_DOOR` и события `DOOR_LOCKED`/`DOOR_UNLOCKED` (запертая не открывается — reason `door_locked`, непроходима для AI/автопути/рывка). Генератор: босс-комната — самый дальний узел дерева (родитель exit-узла), reward — exit-узел за ней, двери коридоров босс-комнаты — `boss_door`, случайный босс из пула спавнится в её центре. Контроллер `world-reactions/boss-room-reaction.ts`: вход игрока при живом боссе внутри → двери закрываются и запираются; выход → отпираются (преследование — hunter-FSM); смерть последнего босса → отпираются насовсем. Концовка `getDefeatedBosses()` — локализованные имена из контента (ключи `ending.boss1..boss4` удалены); лог «Дверь заперта/отперта», «Заперта» в поповере двери. Обновлены `roadMap.md` 1.2/1.3, `first-boss-concept.md`, `mechanics-overview.md` (§6.4, §8, §9, §10), `CONTENT.md`, `src/simulation/AGENTS.md`, `src/content/AGENTS.md`, рецепты `add-room-type.md`/`add-prop.md`. |
| 2026-08-18 | Статусы `bleeding` и `rooted` (концепт этажа 1, п.3–4 §3). `bleeding` — чистый контент: член union `StatusEffectType`, шаблон (категория `wound` — отдельная, чтобы совместное наложение с `rooted` одним батчем не терялось в `resolveStatusBatch`), правило `status_bleeding_tick_damage` (8% maxHp физического урона, режется бронёй, `eventRole: 'target'` против дубля через слой radius), тексты ru/en. `rooted` — движковая семантика (категория `control`): хелпер `isRooted` (`systems/rooted-helper.ts`); `moveEntity.validate` отклоняет MOVE с reason-кодом `actor_rooted`; гарды в исполнителях dash/swoop (нет целей, пустой resolve — в т.ч. для подготовленных AI-способностей); игровой телепорт блокируется (`executeTeleportEntityIntent`, системные — через `ignoreRooted`); PUSH не блокируется (проверки намеренно в действиях/скиллах, а не в `executeMoveIntent`); `decideHunterAction` под rooted не делает шагов (атака только по соседней видимой цели, иначе END_TURN). Presentation/UI: порядок в `statusSorting.ts`, имена в логе (`logBuilder` keyMap + i18n `effectBleeding`/`effectRooted`), иконки-заглушки `statuses/bleeding.png`/`rooted.png`. Тесты: unit `systems/rooted.test.ts` (11), `status-effects/bleeding.test.ts` (3), интеграция `rooted-scenario.test.ts` (4). Обновлены `mechanics-overview.md` (§3), `src/simulation/AGENTS.md`, концепт этажа. Прогоны: typecheck/validate чисты; vitest — 1689 зелёных, 13 падений — те же pre-existing (скейлер скорости анимаций, 2026-08-12). |
| 2026-08-18 | Детализирована механика пращи в `floor-1-content-concept.md` (дизайн-решения, кода не затронуто): базовая атака дальняя с min-range 2 и LOS (п.9 §3), «Бросок камня» — урон + толчок (п.10 §3, `throw` с `pushDistance`), дальние броски расходников — через пассивный фирменный стат `throwRange +5` (п.11 §3 переформулирован; вариант «скилл дальнего броска» отклонён сравнительным разбором), базовая дальность бросков расходников снижается до 2 (§4.6). Отложенный вопрос «Броска бомбы» закрыт — §5 пуст. Заодно устранено повреждение строки сводки (дублированный фрагмент «а черновые.»). |\r
| 2026-08-18 | Реализован п.11 §3 концепта этажа 1 — **стат `throwRange`** (дальность бросков расходников). Код: `'throwRange'` добавлен в `StatNameSchema` (`src/content/schemas.ts`) и union `StatModifier.stat` (`src/simulation/core-types.ts`); `getEffectiveThrowRange` (база 0) в `systems/stats/effective-stats.ts`; новый хелпер `getConsumableThrowRange` (итог = `range` шаблона + стат) читается в валидации `use-item-action` и таргетинге `simulation.ts` (`getConsumableTargetMode`/`getConsumableValidTargets`). Тесты: два кейса в `tests/unit/simulation/actions/use-item-action.test.ts`. Обновлены `mechanics-overview.md` (§1.4, §5), концепт этажа (п.11 §3 закрыт). Прогоны: typecheck/validate:content чисты; vitest — 1691 зелёный, 13 падений — те же pre-existing (скейлер скорости анимаций, 2026-08-12). |
| 2026-08-18 | Код-ревью коммитов `83983c1`/`a70cbdb` и незакоммиченных правок + исправления замечаний. **Категории статусов:** enum `StatusCategory` расширен — `wound` (`bleeding`) и `control` (`rooted`), чтобы совместное наложение одним батчем (мышеловка, концепт §4.7) не теряло статус в `resolveStatusBatch`; тесты в `status-conflict-resolver.test.ts` и `rooted-scenario.test.ts` (реальный контент). **`throwRange`** получил i18n-имя (`SystemStatNamesTranslations` + ru/en `statNames`). **rooted и телепорт:** гард в `executeTeleportEntityIntent`, системные телепорты помечаются `ignoreRooted` (переход этажей в `floor-transition-reaction`). **Контент-правила:** `item_fire_damage_multiplier`/`amulet_fire_damage_multiplier`/`amulet_restore_ap_on_hit` получили `eventRole: 'source'` — фикс повторного срабатывания через слой radius (по прецеденту яда 2026-08-12), регрессионные тесты в `execute-intent-integration.test.ts`; устаревший пункт «условия не оцениваются» в `content-rules/AGENTS.md` исправлен, предупреждение в `CONTENT_RULES_EDGE_CASES.md` расширено на modifyDamage. **Follow-ups комнат:** `bossDoorId` в `MapParamsSchema` (default `boss_door`, валидация ссылки на doors при заданном `bossPool`) вместо хардкода в `buildDoors`; двойное закрытие двери убрано — `executeLockDoorIntent` сам эмитит `DOOR_CLOSED` для открытой двери, реакция босс-комнаты шлёт только `LOCK_DOOR`; актор UNLOCK при смерти босса — `PLAYER_ID`; `spawnTileEffectPatches` исключает клетку старта игрока. Доп. тест: тик/спадание rooted. Обновлены `docs/recipes/add-status.md`, `src/simulation/AGENTS.md`, `src/content/AGENTS.md`, концепт этажа (категории статусов). |
| 2026-08-19 | Выполнен целиком план `docs/plans/ranged-basic-attack-and-throw.md` (концепт этажа 1, п.9–10 §3); файл плана удалён по конвенции (см. запись от 2026-08-13). **п.9 — дальняя базовая атака:** `minRange` (int ≥ 1, default 1) в `WeaponStatsSchema`, ранее мёртвое поле `range` заработало; `AttackAction` получил позиционную форму `targetPosition` рядом с направленной (dx, dy — legacy bump); валидация в `attack-action.ts` (цель на клетке, манхэттен-дистанция ∈ [minRange, range], LOS через `computeFOV`; reason-коды `no_target_at_tile`/`target_out_of_range`/`target_too_close`/`no_line_of_sight`); bump дальнобойного оружия (`minRange > 1`) деградирует в unarmed (хелпер `asUnarmed`, семантика едина для игрока и AI); хелпер `getWeaponAttackRange` (`systems/stats/weapon-range.ts`); API `getBasicAttackTargetMode()`/`getBasicAttackValidTargets()` в `GameSimulation` (манхэттен-фильтр ≥ minRange поверх `getDamageablePositionsWithinRange`; диагональные соседи для melee покрываются направленной формой — MVP-упрощение). UI: режим `basicAttackTargeting` в `gameSession.ts` (по паттерну itemTargeting), слот 0 хотбара закреплён за базовой атакой (`HOTBAR_WEAPON_SLOT_INDEX`, kind `weapon`, автозаполнение со слота 1, клавиша «1», `WeaponSlotPopover`), ПКМ отменяет любой таргетинг; i18n `components.hotbar.weaponSlot*` (ru/en). **п.10 — вид способности `throw`:** новый член union `kind: 'throw'` в `AbilityTemplateSchema` (`range` int ≥ 1, `baseDamage` ≥ 0 плоский — консистентно с `groundSlam`/`swoop`, `pushDistance` int ≥ 0); фабрика `createThrowSkill` (`executors/throwSkill.ts`, ветка в `KIND_FACTORIES`): TargetMode `single{range}`, цели — damageable в LOS, resolve = DAMAGE (теги из шаблона) + цепочка одноклеточных PUSH по `sign(target − caster)`; столкновения — существующие глобальные правила `collision_*`. Тесты: unit `throw.test.ts` (9), `attack-action.test.ts` (12), `basicAttackTargeting.test.ts` (12) + сдвиг индексов в `hotbar.test.ts`. Обновлены концепт этажа (п.9/п.10 выполнены, урон «Броска камня» — плоский `baseDamage`), `src/content/AGENTS.md`, `src/simulation/AGENTS.md`, `docs/agents/CONTENT.md`, рецепты `add-ability.md`/`add-weapon.md`, `mechanics-overview.md`. |\r
| 2026-08-19 | Реализованы правки геометрии по фидбеку плейтеста пращи (roadMap 1.6; анализ и выбор вариантов — `docs/plans/range-geometry-and-targeting-analysis.md`, варианты 1А/2Б/4Б). **Единая метрика — Чебышёв (квадрат):** `isInWeaponRange` и reason-коды в `attack-action.ts` (дублирующий manhattan-хелпер удалён), ромбы в `skills/targeting.ts` (`getVisiblePositionsWithinRange`/`getDamageablePositionsWithinRange`/`getEnemyPositionsWithinRange`), `getWeaponAttackLosRadius` = `range`; FOV стал квадратным — убрано евклидово отсечение в `systems/fov.ts` (shadowcasting остаётся). Замечание 3 закрылось автоматически: `minRange 2` отсекает все 8 соседей. **2Б:** таргетинг `throw` — только цели на 8 лучах-направлениях от кастера (`isOnRay` в `throwSkill.ts`), толчок всегда вдоль луча. **4Б:** bump дальнобойным оружием (`minRange > 1`) отклоняется валидацией с reason-кодом `too_close_for_ranged_weapon` (деградация в unarmed и хелпер `asUnarmed` удалены); тост игроку через ERROR_MAP `toastBuilder.ts` + i18n `components.toast.tooCloseForRangedWeapon*` (ru/en). Тесты: обновлены `attack-action.test.ts`, `throw.test.ts` (8-лучевые кейсы), `fov.test.ts`, `ai-helpers.test.ts`, `hunter-strategy.test.ts`, `use-item-action.test.ts`, `targeting.test.ts`. Обновлены `src/simulation/AGENTS.md`, `src/content/AGENTS.md`, `docs/agents/CONTENT.md`, рецепты `add-weapon.md`/`add-ability.md`, `mechanics-overview.md`, концепт этажа, roadMap 1.6, describe в `schemas.ts`. Прогоны: typecheck/validate чисты; vitest — 1745 зелёных, 13 падений — те же pre-existing (скейлер скорости анимаций, 2026-08-12). |
| 2026-08-20 | Клик по врагу — умный автопуть атаки (melee унифицирован с ranged). **Simulation:** новый query `findNearestAttackPosition(target)` (`simulation.ts`, интерфейс в `types.ts`) — ближайшая к игроку атакующая клетка (проходимая, `isInWeaponRange` до цели, LOS из клетки-кандидата тем же FOV, что валидация атаки; тай-брейк: кратчайший путь → ближайшая по Чебышёву → координаты) + путь до неё; пустой путь = игрок уже в позиции; null = кандидатов нет. **Presentation:** hover на врага в зоне поражения не строит автопуть, а отдаёт `RenderInput.enemyHoverOverlay` (зона `getBasicAttackRangeCells` + цель + флаг `inRange`); клик по врагу в зоне — сразу позиционный ATTACK (dx/dy = Math.sign для анимации, как в `submitTarget`); вне зоны — автопуть до атакующей клетки, финал `AutoPathController.step` — позиционный ATTACK по текущей позиции врага (пересчёт на каждом шаге), bump-финал и условие Чебышёва=1 для врагов упразднены; fallback (нет атакующей клетки) — путь в клетку врага; `computeTurnEndIndices` учитывает финальную атаку на последнем тайле пути. **UI:** `TargetingRenderer` рисует `enemyHoverOverlay` (зона стилем radiusCells, подсветка цели, пунктирная линия от визуального центра игрока). Тесты: `find-nearest-attack-position.test.ts` (7), обновления и новые кейсы в `autoPath.test.ts` (64), `TargetingRenderer.test.ts` (20). Обновлены `mechanics-overview.md` (§1.3), `PRESENTATION_CONTRACT.md` (§2.8), `src/simulation/AGENTS.md`. Прогоны: typecheck чист; vitest unit — 1675 зелёных, 13 падений — те же pre-existing (скейлер скорости анимаций, 2026-08-12); integration — 94 зелёных. |
| 2026-08-20 | Полировка автопути атаки по фидбеку плейтеста. **Линия к врагу:** при цели вне зоны поражения пунктирная линия `enemyHoverOverlay` идёт от последнего шага автопути (атакующей клетки — оттуда будет бить атака), а не от персонажа; в fallback-режиме (путь в клетку самого врага) — по-прежнему от игрока (`TargetingRenderer`). **Приоритет атакующей клетки** в `findNearestAttackPosition` изменён: первичен теперь критерий «визуально ближайшая к игроку» (квадрат евклидовой дистанции), длина пути вторична (тай-брейк при равной дистанции), затем координаты (y, x) — было «кратчайший путь первичен, тай-брейк по Чебышёву», что выбирало визуально далёкие клетки при обходе препятствий. Тесты: обновлены ожидания тай-брейка в `find-nearest-attack-position.test.ts` и `autoPath.test.ts`; новые кейсы — приоритет визуальной близости над длиной пути (melee, стена) и старт линии от последнего шага пути / fallback в `TargetingRenderer.test.ts`. Обновлены `mechanics-overview.md` (§1.3), `PRESENTATION_CONTRACT.md` (§2.8), `src/simulation/AGENTS.md`. Прогоны: typecheck чист; vitest (simulation + presentation + TargetingRenderer) — 1447 зелёных, 0 падений. |
