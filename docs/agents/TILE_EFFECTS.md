# Работа с тайловыми эффектами

> **Статус:** `[STABLE]` — инструкция по добавлению тайловых эффектов актуальна.
> **Источник правды:** этот файл для процесса; `src/simulation/systems/intents/` и `src/simulation/content-rules/` для кода.

> Этот файл — инструкция для агентов. Архитектурное описание системы см. в [`../architecture/TILE_EFFECTS.md`](../architecture/TILE_EFFECTS.md).

---

## Быстрый обзор

Тайловые эффекты — это динамические материалы на клетках карты: вода, масло, огонь и т.п. Они живут в `src/content/templates/tile-effects/` как TypeScript-шаблоны, а своё игровое поведение получают через контентные правила и статусы.

Есть два вида объектов:

- **Тайловый эффект** — сам материал (`water`, `oil`).
- **Статус тайлового эффекта** — временное состояние материала (`burning` на масле).

Чтобы добавить, например, горящее масло, нужно создать и то, и другое, а затем связать их правилами.

---

## Добавление нового тайлового эффекта

В качестве примера используется уже реализованная пара `water` / `oil`.

### Шаг 1. Создать TS-шаблон

Создайте файл `src/content/templates/tile-effects/<идентификатор>.ts`. Имя файла должно совпадать с полем `id` внутри. Шаблон экспортируется как константа:

```ts
import type {TileEffectTemplateInput} from '../../schemas';

export const myEffect = {
  id: 'my_effect',
  // ...остальные поля
} satisfies TileEffectTemplateInput;
```

Основные поля шаблона:

- `layer` — слой эффекта: `cover` (по умолчанию) или `aboveGround`. Уникальность по слою: на клетке максимум один эффект каждого слоя — новый эффект слоя заменяет старый, поэтому вытеснение (например, вода ↔ масло на слое `cover`) настраивается выбором слоя, а не отдельными полями совместимости.
- `blocksLOS` — блокирует ли эффект линию видимости (по умолчанию `false`), например дым на слое `aboveGround`. Движение эффекты не блокируют никогда. Появление/исчезновение такого эффекта автоматически пересчитывает FOV (мировая реакция `tileEffectFovReaction`).
- `concealsEntities` — скрывает ли эффект сущностей на своей клетке (по умолчанию `false`), например взвешанная мука. Скрытая сущность видна наблюдателю только с дистанции ≤ 1 (Чебышёв), симметрично для игрока и AI. FOV-грид при этом не меняется: проверка выполняется в момент запроса через `isEntityConcealedFrom` (`src/simulation/state.ts`) и учитывается в восприятии AI, таргетинге и рендере врагов.
- `duration` — базовая длительность в ходах.
- `renderOrder` — порядок отрисовки относительно других эффектов на той же клетке (внутри своего слоя).
- `ruleIds` — идентификаторы контентных правил, которые активируются при событии на клетке с эффектом.
- `canHaveStatus` — статусы тайловых эффектов, которые можно наложить на этот материал.
- `durationDecreasesWhenHasStatus` — если эффект должен исчезать только при определённом статусе (как масло сгорает только при `burning`).

### Шаг 2. Добавить тексты

Добавьте локализованное название в `src/content/texts/ru/tile-effects.ts` и `src/content/texts/en/tile-effects.ts`. Ключ в объекте — идентификатор эффекта, значение — объект с полем `name`. Без записи шаблон не получит локализованного имени, но игра не сломается: будет fallback на идентификатор.

### Шаг 3. Добавить спрайт

Поместите файл спрайта в `public/assets/tile-effects/<идентификатор>.png`. Рендерер использует соглашение об именовании: путь строится как `/assets/tile-effects/<effectType>.png`.

### Шаг 4. Зарегистрировать в index.ts категории

Добавьте импорт и строку с константой в массив `tileEffectTemplates` в `src/content/templates/tile-effects/index.ts`. Без этого шаблон не попадёт в сборку контента.

### Шаг 5. Добавить контентные правила

Если материал должен что-то делать при входе актора, при получении урона или при тике:

1. Добавьте правило в массив `CONTENT_RULES` в `src/simulation/content-rules/rules.ts`.
2. Укажите идентификатор правила в поле `ruleIds` шаблона тайлового эффекта.

При событии на клетке с эффектом правило автоматически попадёт в мировой слой `tileEffect`, а правило из статуса тайлового эффекта — в слой `tileEffectStatus`.

Глобальные мировые правила, не привязанные к конкретному эффекту, добавляются в массив `GLOBAL_WORLD_CONTENT_RULES` в `src/simulation/content-rules/world-rules/global-rules.ts`.

Исполнители тайловых интентов находятся в `src/simulation/systems/intents/tile-effect-intent-executor.ts` (`SPAWN_TILE_EFFECT`, `APPLY_TILE_EFFECT_STATUS`, `TICK_TILE_EFFECTS` и др.).

Полезные условия, селекторы и эффекты:

- `inTileEffect` — проверяет, что на клетке есть указанный эффект.
- `tileEffectHasStatus` — проверяет, что у эффекта на клетке есть указанный статус.
- `entityHasTag` — проверяет теги шаблона сущности (например, `contains.oil` у пропа).
- `tilesInRadius` — выбирает соседние клетки с указанным эффектом (нужен для распространения).
- `positionsInRadius` — выбирает клетки в радиусе для спавна эффекта.
- `spawnTileEffect` — эффект правила, создающий тайловый эффект в выбранных клетках.

### Шаг 6. Добавить источник эффекта (опционально)

Если эффект должен появляться в обычной игре, рекомендуется делать это через **расходник**. Альтернатива — способность.

#### Через расходник (предпочтительно)

1. Создайте шаблон предмета в `src/content/templates/items/consumables/<id>.ts` (экспорт константы с `satisfies ItemTemplateInput`).
   - `type: "consumable"`.
   - `consumable.effect: "spawn_tile_effect"`.
   - Укажите `tileEffectType`, `radius` (зона спавна) и `range` (дальность броска).
2. Добавьте спрайт в `public/assets/items/<id>.png`.
3. Зарегистрируйте предмет импортом и строкой в массиве `itemTemplates` в `src/content/templates/items/index.ts`.
4. Добавьте название и описание в `src/content/texts/{ru,en}/items.ts`.
5. Анимация броска (`ITEM_THROW`) добавляется автоматически для расходников с `spawn_tile_effect` — отдельный исполнитель не требуется.
6. Для тестов используйте `USE_ITEM` вместо `USE_ABILITY`.

#### Через способность

1. Создайте шаблон способности в `src/content/templates/abilities/<id>.ts` (экспорт константы с `satisfies AbilityTemplateInput`) и зарегистрируйте его в `index.ts` категории.
2. Создайте исполнитель способности в `src/simulation/skills/executors/<id>Skill.ts`, который порождает интенты `SPAWN_TILE_EFFECT`.
3. Зарегистрируйте исполнитель в реестре скиллов.
4. Добавьте анимацию применения в `src/presentation/animation/skills/`.
5. Добавьте текст и спрайт для UI.

### Шаг 7. Написать тесты

Добавьте тесты в одну из папок:

- `tests/unit/simulation/intents/tile-effect-intent-executor.test.ts` — для проверки исполнителей интентов.
- `tests/unit/simulation/content-rules/<правило>.test.ts` — для проверки отдельных правил.
- `tests/integration/tile-effects/<сценарий>.test.ts` — для сквозных сценариев.

Проверьте edge cases: замена эффекта того же слоя при спавне (вытеснение с событиями REMOVED → CHANGED), сосуществование эффектов разных слоёв на одной клетке, обновление длительности при повторном спавне, удаление вместе со статусами.

### Шаг 8. Запустить проверки

После изменений запустите: `npm run typecheck`, затем `npm test`, затем `npm run validate:content`.

---

## Добавление нового статуса тайлового эффекта

Если материал должен временно менять своё поведение (гореть, электризоваться, замерзать), добавьте статус тайлового эффекта.

### Шаг 1. Создать TS-шаблон статуса

Создайте файл `src/content/templates/tile-effect-statuses/<идентификатор>.ts` — экспорт константы с `satisfies TileEffectStatusTemplateInput`. Основные поля:

- `duration` — базовая длительность.
- `ruleIds` — правила, активируемые, когда статус присутствует на клетке события.
- `statusCategory` и `categoryPriority` — для разрешения конфликтов.
- `mutuallyExclusiveWith` и `blockedBy` — конфликты с другими статусами тайловых эффектов.
- `renderOrder` — порядок отрисовки. Обычно статусы рисуются поверх базового материала.
- `neverExpires` — если статус не должен тикать по своей длительности (как `burning` на масле, который гаснет только вместе с маслом).

### Шаг 2. Разрешить статус у материала

Добавьте идентификатор статуса в поле `canHaveStatus` шаблона(ов) тайлового эффекта, на которых он может существовать. Без этого исполнитель наложения статуса отклонит интент.

### Шаг 3. Добавить тексты и спрайт

- Локализованное название — в `src/content/texts/{ru,en}/tile-effect-statuses.ts`.
- Спрайт — в `public/assets/tile-effects/<идентификатор>.png` (рендерер использует ту же директорию, что и для материалов).

### Шаг 4. Зарегистрировать в index.ts категории

Добавьте импорт и строку с константой в массив `tileEffectStatusTemplates` в `src/content/templates/tile-effect-statuses/index.ts`.

### Шаг 5. Добавить правила

Статусы тайловых эффектов обычно реагируют на события `ENTITY_MOVED`, `ENTITY_DAMAGED`, `TILE_DAMAGED`, `TILE_EFFECT_STATUS_TICKED` и другие.

- Используйте условие `tileEffectHasStatus` для проверки наличия статуса на клетке.
- Для распространения на соседние клетки используйте селектор `tilesInRadius` с `targetConditions`, проверяющими наличие нужного материала и отсутствие статуса.

### Шаг 6. Протестировать

Убедитесь, что:

- статус накладывается только на разрешённые материалы;
- статус тикает и удаляется корректно;
- при удалении родительского эффекта статус удаляется без отдельных событий;
- `neverExpires` работает ожидаемо.

---

## Взрывы тайловых эффектов

Если материал должен взрываться при получении статуса, используйте мировую реакцию на событие `TILE_EFFECT_STATUS_APPLIED` и выделенный интент `TILE_EXPLOSION`.

Пример — взрыв горящего масла:

1. Мировая реакция `tileEffectExplosionReaction` (`src/simulation/systems/world-reactions/tile-effect-explosion-reaction.ts`) читает поле `explosion` шаблона тайлового эффекта (`TileEffectTemplateSchema`) и срабатывает, когда наложенный статус совпадает с `explosion.triggerStatus` и `isNew === true`.
2. Реакция порождает `TILE_EXPLOSION` с параметрами урона, радиуса и тегов из шаблона; при `explosion.consumesEffect: true` дополнительно порождает `REMOVE_TILE_EFFECT` (эффект расходуется взрывом).
3. Исполнитель `executeTileExplosionIntent` эмитит событие `TILE_EXPLODED`.
4. Мировая реакция `tileExplosionDamageReaction` (`src/simulation/systems/world-reactions/tile-explosion-damage-reaction.ts`) превращает `TILE_EXPLODED` в `DAMAGE_TILE` по всем клеткам в радиусе (включая центр).

Все `DAMAGE_TILE` от одного взрыва исполняются одной волной: сначала урон наносится по всем клеткам, затем на все `TILE_DAMAGED` срабатывают реакции поджога. Благодаря этому огонь распространяется параллельно во все стороны.

Поле `isNew` в событии `TILE_EFFECT_STATUS_APPLIED` позволяет отличить первое наложение статуса от обновления длительности и избежать повторных взрывов.

Добавляя новый взрывающийся материал, новая реакция не нужна: задайте поле `explosion` в шаблоне тайлового эффекта (чистый контент, см. `src/content/templates/tile-effects/oil.ts`).

---

## Частые ошибки

- **Забыли добавить `canHaveStatus`.** Статус не наложится, даже если правило сработает.
- **Забыли `ruleIds` в шаблоне.** Правило существует, но не активируется на клетке с эффектом.
- **Статус не добавлен в index.ts категории.** Шаблон не попадёт в сборку контента, и игра не сможет его найти.
- **Спрайт положили не в ту папку.** Рендерер ищет спрайты только в `public/assets/tile-effects/`.
- **`neverExpires` без `durationDecreasesWhenHasStatus`.** Материал никогда не исчезнет, если статус не снимается другим способом.

---

## Связанная документация

- [`../architecture/TILE_EFFECTS.md`](../architecture/TILE_EFFECTS.md) — архитектурное описание системы.
- [`CONTENT.md`](./CONTENT.md) — общий контент-пайплайн.
- [`CONTENT_RULES_EDGE_CASES.md`](./CONTENT_RULES_EDGE_CASES.md) — крайние случаи контентных правил.
- [`src/simulation/AGENTS.md`](../../src/simulation/AGENTS.md) — локальные правила слоя simulation.
- [`src/simulation/content-rules/AGENTS.md`](../../src/simulation/content-rules/AGENTS.md) — локальные правила слоя content-rules.
