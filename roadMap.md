# Mousefall — Roadmap крупными мазками

> Черновик плана на ревью.  
> Цель: из текущего прототипа с 10 одинаковыми уровнями сделать полноценный 10-этажный roguelike.

---

## Видение

- **10 этажей**, каждый — уникальный биом с собственными пулами врагов, предметов и боссов.
- **Босс на каждом этаже**: случайный из пула этого этажа/биома. Переход на следующий этаж — только после его убийства.
- **Прокачка внутри забега**: через предметы — заменяемая экипировка + накапливаемые реликвии. Реликвия выбирается (1 из 3) на алтаре выбора в начале каждого этажа. Классического XP/level-up нет. См. [`docs/game-design/progression-concept.md`](./docs/game-design/progression-concept.md). Реализовано в этапе 0 (2026-08-06).
- **Экономика**: золото с врагов/сундуков/боссов, магазин между этажами, возможный крафт/улучшение снаряжения.
- **Мета-прогрессия**: нет. Сейчас — «трушный рогалик».
- **UI/анимации и сохранения**: в последнюю очередь, после стабилизации игрового цикла.

---

## Отложенные доработки

> Решения, отложенные при планировании этапов (2026-08-04), с привязкой к этапам.

**Этап 1.3:**
- ~~Выбрать вариант щита Кота-Стража~~ — решено 2026-08-10 при дизайне босса: периодическая активируемая неуязвимость (Глухая оборона), см. [`docs/game-design/first-boss-concept.md`](./docs/game-design/first-boss-concept.md).
- Немедленная реакция перехода стадий босса (прерывание в момент пересечения порога 50% HP, в т.ч. посреди хода игрока) — после полной реализации босса; MVP — переход в начале хода босса.

**Этап 2:**
- Судьба стата `luck`: пока оставлен, ни на что не влияет; на этапе 2 определить влияние на игру, возможно — выпилить или переделать под «харизму».
- Сундуки (в т.ч. в комнате награды): реализовать вместе с экономикой; до этого награда — пул лута босса.
- Полное/частичное восстановление HP между этажами (например, через магазин/торговца) — этап 2 или 3.

**Этап 3:**
- Дополнительные условия крита (например, `wet` + электричество, удар по цели вне поля зрения) — контентом, по мере появления релевантных механик.
- Додж-заряды (уклонение как расходуемый ресурс) — ниша для отдельного врага/босса/реликвии (этап 3 или 5).
- «Рычаг» босс-комнаты вместо автозакрытия: двери закрывает босс при обнаружении игрока; игрок может дёрнуть тот же рычаг, чтобы открыть двери.
- Тип комнаты `rest` (комната отдыха: лечение, расходники, без врагов).
- Дополнительное восстановление HP через регенерацию или сон (связка с комнатой `rest`).
- Модификаторы комнат — «случайные дополнительные условия» поверх базовых типов комнат (вариант 2).
- Не-floor террейн (песок) + учёт `moveCost` в AI и автопути игрока.
- Префабы комнат (ручные раскладки, сетпieces) — **в конец этапа 3**.

**Этап 6:**
- Красивый фидбек блока щитом: отдельный эффект/событие «урон заблокирован» (механически достаточно `modifyDamage multiply 0`).

---

## Этап 1. Первый этаж + первый босс

> Цель: есть работающий вертикальный срез — один полноценный этаж с боссом, который можно пройти.

### 1.1 Скиллы стартового снаряжения (Перед этим доработать эги в оружии. Переделать тип урона под тэги)
> Цель: у каждого вида начального снаряжения есть осмысленный выбор из 2 активных скиллов.

- Создать 2 скилла для оружия (например, сильный удар и серия ударов).
- Создать 2 скилла для брони (например, блок и контратака).
- Создать 2 скилла для амулета (например, лечение и бафф).
- Оформить все скиллы как `AbilityTemplate` и привязать к стартовым предметам через `grantedAbilities`.
- Проверить, что скиллы корректно выдаются при старте забега и отображаются в панели скиллов.

### 1.2 Генерация и параметры первого этажа
- Генерация этажа переводится на **типы комнат**: тип комнаты = контентный шаблон (вес, ограничения размещения, пулы и плотности наполнения, гарантированные объекты); генератор назначает типы узлам дерева комнат и наполняет каждую по её шаблону. **Реализовано (2026-08-14)**: категория `roomTypes` (`src/content/templates/room-types/`), вид `generated`; плотности — от площади комнаты; лужи масла/воды спавнятся генератором; `Room.roomTypeId` в состоянии этажа. План: `docs/plans/room-types-generation.md`. Осталось: тип `elite`, вид `preset` для комнат, биом этажа.
- Стартовый набор типов этажа 1: `start` (алтарь выбора реликвии), `normal` (обычные враги, лут, колючки по пулам) — **реализованы**; `boss` (босс-комната, назначается генератором напрямую) и `reward` (лестница + алтарь лечения) — **реализованы (2026-08-14, см. 1.3)**; `elite` (сильные враги, бочки с маслом, повышенная награда) — впереди.
- ~~Генератор сохраняет границы и типы комнат в состояние этажа~~ — **сделано** (`Room.bounds` было, добавлен `Room.roomTypeId`).
- Задать `MapParams` для 1-го этажа: размер, плотность врагов, плотность предметов.
- Определить визуальный стиль/биом первого этажа.
- Спавн врагов и предметов по пулю для 1-го этажа.

### 1.3 Первый босс — Кот-Страж
- **Концепция:** тяжеловооружённый босс-первого этажа. Механики утверждены: см. [`docs/game-design/first-boss-concept.md`](./docs/game-design/first-boss-concept.md) (способности Глухая оборона / Налёт / Удар по земле, две стадии с переходом на 50% HP). Концепт заменяет исходную заготовку «щит/парирование» ниже: вариант щита выбран (периодическая неуязвимость — Глухая оборона), парирование исключено.
- **Статус:** механики босса реализованы (2026-08-10/11): статус `bulwark` + движковый иммунитет, три способности (`bulwark`, `guardian_swoop`, `ground_slam`), AI-стратегия `guardian-boss` (две стадии, комбо «Удар + Оборона», тактика столкновения Налётом), контент `cat_guardian`. Инфраструктура босс-файта реализована (2026-08-14) — босс спавнится в босс-комнате, бой идёт по полному циклу; нижележащие пункты выполнены (план: [`docs/plans/boss-fight-infrastructure.md`](./docs/plans/boss-fight-infrastructure.md)).
- **Механики (историческая заготовка, заменена концептом):**
  - **Щит:** пассивное снижение входящего урона или периодическое активирование неуязвимости (вариант — при дизайне босса, см. «Отложенные доработки»).
  - **Парирование:** при получении удара в определённом состоянии наносит ответный урон.
  - **Тяжёлая броня:** высокое здоровье и броня, низкая скорость.
- **Принцип:** босс реализуется контентом на существующей системе правил, **без босс-специфичного кода** — нужен скилл → добавляем скилл, предмет → предмет, правило → правило (правила привязываются к врагу через экипировку и статусы). ~~Единственная доработка — обобщённый исполнитель self-buff способности (`statusType`/`duration` из шаблона способности) вместо захардкоженного `counterattackSkill`~~ — выполнено 2026-08-10 (поле `selfBuff` шаблона + fallback в `getSkillExecutor`; актуализировано 2026-08-11: вид `kind: 'selfBuff'` в union шаблона + фабрика `KIND_FACTORIES`). ~~Семантика `multiply 0` против «минимум 1 урона»~~ — вопрос снят 2026-08-10: пол min-1 удалён из расчёта урона (сохранён только для процентного урона через `min: 1` в правилах), `multiply 0` даёт 0.
- **Босс как контент:** ~~флаг `isBoss` в шаблоне сущности заменяет захардкоженный `BOSS_TEMPLATE_IDS`; экран концовки переводится на имена боссов из контента. Пул боссов — `bossPool` в `MapParams` (по образцу `enemyPool`/`itemPool`), генератор гарантированно размещает случайного босса из пула.~~ — **реализовано (2026-08-14)**: `isBoss` в `EntityTemplateSchema`, `BOSS_TEMPLATE_IDS` удалён, концовка берёт локализованные имена боссов из контента (fallback `unknownBoss`); `bossPool` в `MapParamsSchema` (при заданном пуле генератор спавнит случайного босса в центре босс-комнаты).
- **Босс-комната (гейт перехода):** ~~генератор помечает дальнюю от спавна комнату босс-комнатой и строит за ней комнату награды, достижимую только через босс-комнату; в комнате награды — лестница вниз и алтарь лечения (до реализации сундуков награда — отдельный пул лута босса).~~ — **реализовано (2026-08-14)**: типы комнат `boss` (weight 0, родитель exit-узла дерева) и `reward` (exit-узел за ней, лестница вниз остаётся в ней, `guaranteedPois: ['altar']`).
- **Двери босс-комнаты:** ~~особый шаблон — неразрушаемые, негорючие, с новым универсальным состоянием двери «заперта». Логика: игрок вошёл в босс-комнату → двери закрываются и запираются (триггер — сам факт входа, безусловно); все боссы комнаты мертвы → отпираются насовсем; игрок любым способом вышел до победы → отпираются, при этом урон боссу **сохраняется** (без сброса HP), а босс выходит из комнаты и **преследует** игрока.~~ — **реализовано (2026-08-14)**: шаблон `boss_door` (тег `boss_room`, `indestructible` — движковое обнуление урона, негорючая), универсальное состояние `DoorEntity.isLocked` (интенты `LOCK_DOOR`/`UNLOCK_DOOR`, события `DOOR_LOCKED`/`DOOR_UNLOCKED`); контроллер `world-reactions/boss-room-reaction.ts` запирает двери при входе игрока при живом боссе внутри, отпирает при его выходе (преследование — существующий hunter-FSM стратегии босса) и насовсем после смерти последнего босса. Уточнение триггера: запирание — только при живом боссе внутри комнаты (иначе сломалось бы преследование). Отложенная доработка: закрытие по «рычагу» (см. «Отложенные доработки», этап 3).

### 1.4 Базовый контент 1-го этажа
- Пул врагов для 1-го этажа (можно на основе текущих + 1 новый).
- Пул предметов для 1-го этажа.
- Базовые расходники. Три реализованных типа эффектов (`heal`, `buff`, `spawn_tile_effect`) остаются как есть; при дизайне определяются состав расходников, судьба заглушек (`damage`, `teleport`, `identify`) и обобщение `buff` на `statusType` из шаблона. Ориентиры: `teleport` — осмысленный способ побега из запертой босс-комнаты (связка с преследованием босса, 1.3); боевая `damage`-бомба и свитки — этап 2.3.
- Лечение — **редкий, дефицитный ресурс**: алтарь восстановления HP (комната награды) + редкие расходники лечения. Конкретные числа (бюджет лечения на этаж, стартовый инвентарь) — при балансном проходе этого пункта и плейтесте.
- Баланс значений/длительностей яда и деза, иммунитеты врагов — этим же проходом.
- Слияние стопок при подборе: слияние в существующую стопку до `maxStack` + валидация «`stackable` несовместим с `abilityPool`» (экземпляры с ролленными способностями не сливаются) + тесты. Разделение стопок и частичный перенос — этап 2.3.

### 1.5 Условие победы в забеге
- **Реализовано (2026-08-24)**: спуск по лестнице вниз с финального этажа завершает забег победой. Глубина забега — контентная ручка `MapParams.finalFloor` (default = `MAX_FLOOR`; у `floor_1` — 1, т.е. финальная победа временно = прохождение 1-го этажа). `interact-action` на финальном этаже порождает новый интент `COMPLETE_RUN` вместо `FLOOR_TRANSITION`; исполнитель (`complete-run-intent-executor.ts`) выставляет `phase = 'victory'` и эмитит событие `RUN_COMPLETED`, новый этаж не генерируется. Отказ `max_floor_reached` удалён вместе с тостом и i18n-ключами; Presentation/UI уже обрабатывали фазу `victory` (`gameSession`, `EndingScreen`). Переход на следующий этаж для нефинальных этажей работает как раньше.

### 1.6 Правки геометрии таргетинга и дальних атак (фидбек плейтеста 2026-08-19, праща)

> **Реализовано (2026-08-19)** по разбору [`docs/plans/range-geometry-and-targeting-analysis.md`](./docs/plans/range-geometry-and-targeting-analysis.md) — выбраны варианты 1А / 2Б / 4Б:

1. **Ромбовидный радиус каста/атаки** → боевая геометрия переведена на **Чебышёва (квадрат)**: дальности оружия, таргетинг скиллов и бросков расходников, радиус обзора (FOV без евклидова отсечения) — единая метрика с 8-направленным движением и AOE.
2. **Толчок «Броска камня» по вектору цели** → таргетинг `throw` только по **8 лучам-направлениям** (как у рывка); толчок всегда строго вдоль луча.
3. **Мёртвая зона пращи неполная** → закрыто автоматически п.1: `minRange 2` по Чебышёву отсекает все 8 соседних клеток.
4. **Несогласованность bump и подготовки** → bump дальнобойным оружием (`minRange > 1`) отклоняется валидацией с сообщением игроку («Этим оружием нельзя бить в упор», reason-код `too_close_for_ranged_weapon`); деградация в безоружный удар удалена.

**Критерий готовности:** можно начать забег и пройти 1 этаж, убив босса.

---

## Этап 2. Экономика

> Цель: в игре появляются золото, магазин и работающие стопки предметов. Прокачка персонажа реализована в этапе 0 (реликвии, см. `progression-concept.md`); XP/level-up из плана исключён.

### 2.1 Золото
- Золото с врагов, сундуков, боссов.
- Отображение в UI.

### 2.2 Магазин
- Экран торговца между этажами.
- Покупка/продажа предметов через `ItemTemplate.value`.
- Ассортимент привязан к текущему/следующему этажу.
- Возможность продажи реликвий в магазине — по отложенной доработке концепта прогрессии (ассортимент с прямым выбором, привязка к этажу).

### 2.3 Расходники и стопки
- Разделение стопок и частичный перенос (слияние при подборе — этап 1.4).
- Расширение расходников: зелья, бомбы, свитки.

**Критерий готовности:** в забеге работают золото, магазин и стопки предметов.

---

## Этап 3. Контент для ещё 2 этажей

> Цель: итого 3 рабочих этажа с разными биомами/врагами/боссами.

### 3.1 Этажи 2 и 3
- Параметры генерации для этажей 2 и 3.
- 2 новых биома.
- Пулы врагов и предметов под каждый биом.

### 3.2 Новые враги
- 2–3 новых врага на биом.
- Уникальные AI-стратегии и скиллы.

### 3.3 Новые боссы
- По 1 боссу на каждый новый биом.
- Уникальные способности, возможно фазы.

### 3.4 Предметы и скиллы
- Новые предметы для новых биомов.
- Новые активные скиллы для игрока.
- `ITEM_SKILLS`: активные скиллы от экипируемых предметов.

**Критерий готовности:** можно пройти 3 этажа, каждый заметно отличается от предыдущего.

---

## Этап 4. Крафт / улучшения

> Цель: снаряжение становится глубже — можно улучшать, чинить, комбинировать.

### 4.1 Система улучшений
- Улучшение оружия/брони/аксессуаров.
- Стоимость в золоте и/или материалах.

### 4.2 Материалы и ресурсы
- Ресурсы с врагов/сундуков/боссов.
- Простая система крафта/сборки.

### 4.3 Durability и износ
- **CHEESE_ARMOR — сырная броня.** Броня из сыра даёт навык «Укус»: персонаж откусывает кусок брони, восстанавливая здоровье, но уменьшая прочность брони. При прочности 0 броня съедается и исчезает.
  - Добавить `CheeseArmor` с полем `durability`.
  - Реализовать навык `BiteSkill`, доступный только при надетой сырной броне.
  - Применить лечение к сущности и износ к броне; уничтожить предмет при прочности 0.
- Ремонт предметов.
- Универсальная механика durability для других предметов (при необходимости).

### 4.4 Баланс экономики
- Цены, доход с этажа, инфляция, стоимость улучшений.

**Критерий готовности:** крафт/улучшение интегрированы в цикл и используются игроком.

---

## Этап 5. Весь контент для 10 этажей

> Цель: довести количество этажей до 10 с уникальными биомами, врагами и боссами.

### 5.1 Этажи 4–10
- Параметры генерации для каждого этажа.
- Новые биомы или вариации существующих.
- Пулы врагов/предметов для каждого этажа.

### 5.2 Враги и боссы
- Добавить недостающих врагов (итого ~6–9 уникальных + вариации).
- Добавить недостающих боссов (итого 10 уникальных боссов).
- Финальный босс 10-го этажа.

### 5.3 Скиллы и предметы
- Добавить скиллы для новых врагов и боссов.
- **AREA_RAIN_SKILL — каменный/огненный дождь.** Скилл, кастуемый на область; зона наносит урон находящимся в ней существам несколько ходов.
  - Параметры: радиус, длительность, урон за ход, тип урона, кулдаун.
  - Вариации: каменный дождь (физический урон / замедление / оглушение) и огненный дождь (огненный урон / горение).
  - Визуализация зоны на карте; AI должен учитывать зону при перемещении.
- Расширить ассортимент предметов.
- Заполнить `grantedAbility` у экипируемых предметов.

### 5.4 Плавная прогрессия сложности
- Баланс HP/урона/золота по этажам, баланс пулов реликвий по этажам.
- Усиление врагов и боссов ближе к 10-му этажу.

**Критерий готовности:** можно пройти все 10 этажей, каждый этаж уникален.

---

## Этап 6. Полировка + красивый UI

> Цель: игра стабильна, сбалансирована и приятна визуально.

### 6.1 Баланс
- Балансировка всех 10 этажей.
- Баланс врагов, боссов, предметов, прокачки, экономики.
- Плавный рост сложности + пики на боссах.

### 6.2 Сохранения
- Сериализация `GameState`.
- Сохранение прогресса забега (mid-run save).
- Загрузка.

### 6.3 UI и анимации
- Красивый UI.
- Улучшенные анимации и VFX.
- **PARTICLE_BURST — эффекты частиц.** Реализовать шаг анимации `PARTICLE_BURST` (тип уже есть в `src/presentation/types.ts`).
  - Временные `Graphics` с разлетающимися частицами в точке `(x, y)`.
  - Использовать для: крови при уроне, вспышки при смерти, искр при ударе об стену.
  - Добавить `particleExecutor.ts`, зарегистрировать в `GameField`, порождать в `animationPlanner.ts`.
- Визуальные эффекты скиллов, статусов, боссов.
- Настройка скорости анимаций.

### 6.4 Прочее
- Звук/музыка (если планируется).
- Туториал/подсказки.
- Оптимизация производительности на больших уровнях.

**Критерий готовности:** игра стабильна, сохраняется, сбалансирована и визуально завершена.

---

## Что уже есть и не требует переработки

- Action / Intent / Event tree в симуляции.
- AP-система, бой, экипировка, инвентарь, статусы.
- FOV, pathfinding, базовый AI.
- Content pipeline (TypeScript-шаблоны + Zod + реестр).
- Data-driven content rules.
- `activeRules` / кэш активных правил.
- Боевая система на основе реакций (детерминированная: всегда попадание, крит по условию `dazed`/`stunned`).
- Presentation layer: `GameSession`, анимационный планировщик, лог.
- UI: экраны, PixiJS-рендерер, ввод.
- Прогрессия внутри забега: реликвии + алтарь выбора 1 из 3 (этап 0, выполнен 2026-08-06).

## Главные риски / технический долг

- Сохранения отсутствуют полностью.
- Все этажи генерируются по одним параметрам.
- Механики первого босса и инфраструктура босс-файта реализованы (способности, стратегия `guardian-boss`, босс-комната с запирающимися дверями — этап 1.3, 2026-08-14); условие победы реализовано (этап 1.5, 2026-08-24: спуск с финального этажа `MapParams.finalFloor` → `phase = 'victory'`).
- Стопки предметов не работают (слияние при подборе — этап 1.4).
- `value` предметов нигде не используется.
