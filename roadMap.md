# RoadMap Mousefall

> Файл для накопления идей, фич и технических долгов.
> Приоритет: `P0` — критично, `P1` — важно, `P2` — желательно, `P3` — можно отложить.

---

## Анимации и VFX

### `PARTICLE_BURST` — эффекты частиц (P3)

**Описание:** Реализовать шаг анимации `PARTICLE_BURST` (тип уже есть в `src/presentation/types.ts`).
Создавать временные `Graphics` с разлетающимися частицами в точке `(x, y)`.

**Где использовать:**
- Кровь при получении урона (`ENTITY_DAMAGED`).
- Вспышка при смерти (`ENTITY_DIED`).
- Искры при ударе об стену.

**Что нужно сделать:**
1. Добавить `particleExecutor.ts` в `src/ui/animation/`.
2. Регистрация в `GameField` (список executors для `AnimationSequencer`).
3. Порождение `PARTICLE_BURST` в `animationPlanner.ts` (например, как child к `DAMAGE` / `DEATH`).
4. Добавить метод в `WorldRenderer` для временного контейнера частиц.

**Зависимости:** `runTickerTween` (PixiJS Ticker) уже реализован.

### Смешные визуальные эффекты статусов (P3)

**Описание:** Добавить комичные визуальные отклонения в анимации сущностей в зависимости от наложенных статус-эффектов. Это усилит фантазию игры и сделает статусы более заметными без текста.

**Идеи эффектов:**
- **Опьянение / Головокружение:** при анимации `MOVE` спрайт персонажа наклоняется на большой угол (например, ±25–40°), словно он идёт вразвалочку. Направление наклона может чередоваться от шага к шагу или зависеть от направления движения.
- **Страх / Паника:** спрайт дрожит (лёгкий тремор по X/Y) перед ходом или во время idle.
- **Ярость:** спрайт слегка увеличивается в масштабе на долю секунды при начале атаки.
- **Замедление / Оцепенение:** спрайт «прилипает» к полу на мгновение перед движением, затем резко рывком перемещается (stop-motion эффект).

**Где использовать:**
- В анимационных executor'ах (`moveExecutor`, `attackExecutor`) — чтение текущих статусов сущности из `GameState`.
- В `animationPlanner.ts` — возможно, передача модификаторов шага в `AnimationStep`, если эффекты сложнее, чем просто угол.

**Что нужно сделать:**
1. Продумать, как executor'ы получают доступ к статусам сущности во время анимации (через `AnimationContext` или `RenderState`).
2. Реализовать параметризацию угла / масштаба / смещения в существующих tween'ах движения.
3. Убедиться, что эффекты не ломают позиционирование FOV и таргетинг-оверлеев.

---

## Броня и предметы

### `CHEESE_ARMOR` — сырная броня (P2)

**Описание:** Добавить броню, изготовленную из сыра, которая даёт навык **«Укус»**. При использовании навыка персонаж откусывает кусок собственной брони: восстанавливает себе здоровье, но прочность брони уменьшается. Когда прочность брони падает до нуля, броня полностью съедается и исчезает.

**Идея механики:**
- Броня имеет показатель прочности (например, 3 укуса).
- Навык «Укус» доступен только при надетой сырной броне.
- Каждый укус восстанавливает фиксированное количество здоровья (например, 2 HP).
- Каждый укус уменьшает прочность брони на 1.
- При прочности 0 броня уничтожается и навык «Укус» больше недоступен.

**Где использовать:**
- В системе экипировки сущностей (`src/simulation/`).
- В планировщике анимаций — эффект откусывания куска брони.
- В UI инвентаря / слота брони.

**Что нужно сделать:**
1. Добавить тип предмета `CheeseArmor` с полем `durability`.
2. Реализовать навык `BiteSkill`, который проверяет наличие сырной брони в слоте.
3. Применить лечение к сущности и урон/износ к броне.
4. Уничтожить предмет брони при прочности 0 и убрать навык.
5. Добавить визуальный эффект откусывания (частицы / анимация).

---

## Инвентарь и экономика

### `stackable` / `maxStack` — система стопок предметов (P1)

**Описание:** Поля `stackable` и `maxStack` есть в схеме `ItemTemplate`, но в коде не используются. При поднятии предмета инвентарь всегда создаёт новую запись (`InventoryItem`), даже если такой предмет уже есть. Нужно реализовать логику объединения одинаковых предметов в одну стопку с учётом `maxStack`.

**Что нужно сделать:**
1. При `PICK_UP` проверять, есть ли в инвентаре предмет с тем же `templateId` и без `grantedAbility`.
2. Если есть — увеличивать `quantity` существующей записи, не превышая `maxStack`.
3. Если `maxStack` превышен — создавать вторую запись.
4. Обновить `remove-item-intent-executor`: при `quantity > 1` уменьшать счётчик, а не удалять запись.

---

### `value` — цена предмета и торговля (P2)

**Описание:** Поле `value` («цена в золоте») объявлено в `ItemTemplate`, но нигде не читается. Нужна система для использования цен: хотя бы базовый магазин или возможность продажи лута.

**Что нужно сделать:**
1. Добавить счётчик золота у игрока (или отдельный предмет `gold`).
2. Реализовать экран торговца / лавку между этажами.
3. Использовать `template.value` как базовую цену покупки / продажи.

---

## Локализация и тексты

### Вынести все тексты в отдельные файлы (P1)

**Описание:** Все пользовательские тексты (названия, описания, подсказки, сообщения, UI-лейблы и т.д.) сейчас разбросаны по исходному коду. Необходимо централизовать их во внешних файлах (например, JSON или TS-модули под `src/content/`), чтобы упростить поддержку, правки и будущую локализацию.

**Что нужно сделать:**
1. Провести аудит текущих захардкоженных строк в `src/`.
2. Создать единую структуру файлов с текстами (по доменам: `ui`, `entities`, `skills`, `items` и т.д.).
3. Заменить все строки в коде на импорты из файлов текстов.
4. Убедиться, что новые тексты не дублируются и имеют понятные ключи.

---

*Добавляйте новые пункты в соответствующие разделы с указанием приоритета.*
