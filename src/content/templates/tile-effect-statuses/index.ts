import type {TileEffectStatusTemplateInput} from '../../schemas';
import {burning} from './burning';

/** Все шаблоны категории «tileEffectStatuses». Новый шаблон добавляется сюда импортом и строкой в массиве. */
export const tileEffectStatusTemplates: TileEffectStatusTemplateInput[] = [
  burning,
];
