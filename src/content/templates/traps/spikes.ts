import type {TrapTemplateInput} from '../../schemas';

export const spikes = {
  "id": "spikes",
  "ruleIds": [
    "spikes_deal_damage"
  ],
  "oneShot": true,
  "initiallyHidden": true,
  "tags": []
} satisfies TrapTemplateInput;
